# Changelog

## [54] agent-config-ui-ux-overhaul — 2026-04-21

**Why:** The agent configure pages (General and QnA) had accumulated several UX inconsistencies and layout issues: AI helper boxes cluttered every section with "Describe the change" popups that weren't needed; the metrics section had no visible constraint (users could add unlimited metrics despite a 4-slot backend limit); the voice/language picker was an inline expandable panel that pushed content down awkwardly; the Session/Evaluation tabs had mismatched save button placement; and the QnA config page diverged significantly from the General agent layout with no voice/language support, a centralized questions view, and underline tabs instead of pills.

**What:** A comprehensive redesign of both agent configure views. All AI helper boxes and Sparkles buttons removed. Metrics rebuilt as 4 fixed editable slots with color-coding for original vs. changed values and a live counter badge. Voice/language picker extracted into a reusable centered modal accessible from a gear icon in the header. Headers consolidated into a single sticky row with title, pill tabs, language badge, settings, and save. Evaluation tab switched to a 2-column layout (eval prompt main + metrics sidebar). QnA config aligned to match General Agent: pill tabs, voice/language modal, 2-column Profile tab with collapsible sections, compact scrollable Questions tab, and matching Evaluation layout.

**Changes:**
- `src/components/agentspace/wizard/VoiceSettingsModal.tsx` — **new**: reusable centered modal for voice & language selection; dark backdrop + blur, language dropdown, voice cards grid with gender badges and preview buttons, Cancel/Update Voice footer; uses framer-motion AnimatePresence for enter/exit animations
- `src/pages/AgentSpace.tsx` — live link copy button: `text-emerald-500` when agent is `live`, `text-gray-300` when `idle`; test link copy button: `text-orange-400` always; applied to both mobile cards and desktop rows
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — **header**: title + pill tabs (Session / Evaluation) + language badge (`text-[10px] uppercase indigo-700 bg-indigo-50`) + settings gear icon (opens VoiceSettingsModal) + Save Changes button, all in one sticky row; mobile secondary row shows voice name + language + Voice button
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — **voice**: removed inline `VoiceBar` component entirely; voice/language now changed via `VoiceSettingsModal` triggered from header gear icon
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — **AI helpers removed**: subtitle "Edit sections directly or use AI..." removed; `EvalAiButton` component removed; `AiButton` component removed; `SectionHeader` simplified to plain label; all `onAiRewrite` prop plumbing removed from `StringSection`, `DictSection`, `CollapsibleSection`
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — **metrics redesign**: 4 editable `<input>` slots in a grid; original metrics (from `initialMetrics`) render with mild gray styling (`bg-gray-50 border-gray-200 text-gray-600`); new/changed metrics render with soft indigo accent (`bg-indigo-50 border-indigo-100 text-indigo-700`); counter badge `X/4` in header turns emerald when full; empty slots show `+` placeholder; "Maximum 4 metrics reached" text removed
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — **evaluation layout**: switched from single-column to 2-column on `md+` screens; left column (flex-1, order-2 on md) holds Eval Prompt textarea (rows=10); right column (w-[280px] sidebar, order-1 on md) holds the 4 metric slots
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — **save consistency**: single Save Changes button in header works for both Session and Evaluation tabs; Evaluation tab exposes `save()` via `useImperativeHandle` so the parent can call it; per-tab save states (`sessionSaving`/`sessionSaved`/`evalSaving`/`evalSaved`) drive button copy (Saving…/Saved/Save Changes)
- `src/components/agentspace/wizard/QnAConfigureView.tsx` — **header**: redesigned to match General Agent — title + pill tabs (Profile / Questions / Evaluation) + language badge + gear icon + Save Changes button in one sticky row; underline tabs replaced with pill tabs in `bg-gray-100` container
- `src/components/agentspace/wizard/QnAConfigureView.tsx` — **tab order**: changed from `Questions | Profile | Evaluation` to `Profile | Questions | Evaluation`
- `src/components/agentspace/wizard/QnAConfigureView.tsx` — **voice/language added**: `VoiceSettingsModal` wired in; `agent.agent_language` and `agent.agent_voice` now displayed and editable (previously ignored despite being present on the `Agent` type); mobile secondary row with voice info + Voice button
- `src/components/agentspace/wizard/QnAConfigureView.tsx` — **AI helpers removed**: `AiRewritePopup` component removed; `SectionAiButton` component removed; "Regenerate Evaluation" block with Sparkles Generate button removed; all AI rewrite state and API calls (`handleRewrite`, `handleGenerateEval`) removed
- `src/components/agentspace/wizard/QnAConfigureView.tsx` — **profile layout**: switched from single-column stacked cards to 2-column `md:flex-row` layout matching General Agent's Session tab; left column (55%) holds Identity & Persona + Session Brief; right column holds Behavior Rules + Guardrails as collapsible cards with `ChevronDown` animation via `AnimatePresence`
- `src/components/agentspace/wizard/QnAConfigureView.tsx` — **questions compactness**: question items use `truncate` on display text, `rows=1` when editing, and tighter padding (`py-2` instead of `py-2.5`); both Fixed and Randomized columns are scrollable (`max-h-[360px] overflow-y-auto`); layout changed from centered (`max-w-4xl mx-auto`) to full-width (`px-5 py-5`)
- `src/components/agentspace/wizard/QnAConfigureView.tsx` — **evaluation layout**: switched to 2-column on `md+` matching General Agent; left column holds Eval Prompt textarea; right sidebar (w-[280px]) holds 4 metric slots with same original-vs-new color coding
- `src/components/agentspace/wizard/QnAConfigureView.tsx` — **save consistency**: single Save Changes button in header works for all three tabs (Questions/Profile/Evaluation); `handleHeaderSave` dispatches to the correct save handler per active tab

---

## [53] trial-gating-and-balance-visibility — 2026-04-21

**Why:** Two UX gaps after the payment flow launched: (1) The Trial card was still shown as a "Downgrade" option to users who had already used a trial and then upgraded — the frontend was checking only the *current* subscription tier, missing the upgrade-from-trial case. (2) The token balance ring gauge and credit count in the header remained visible even when a subscription was cancelled or expired, showing stale data against a plan with no access.

**What:** Trial card is now filtered using a `had_trial` boolean returned by the backend (which checks both the current subscription row and `subscription_event` history). Balance bar is hidden when the subscription is not active via a new `subscriptionActive` field in `TokenContext`.

**Changes:**
- `src/lib/api.ts`: added `had_trial: boolean` to `AgentspaceSubscription` interface
- `src/context/TokenContext.tsx`: added `subscriptionActive: boolean` to context interface, default, and provider value (`subscription?.status === 'active'`)
- `src/components/agentspace/TokenBalanceBar.tsx`: returns `null` when `!subscriptionActive`, hiding the gauge for cancelled/expired plans
- `src/components/agentspace/PlanPanel.tsx`: trial filter now uses `subscription.had_trial` instead of `subscription.plan_tier === 'trial'` — correctly hides trial after upgrade
- `src/components/agentspace/NoActivePlanScreen.tsx`: same `subscription?.had_trial` filter applied to the plan grid

---

## [52] payment-flow-fixes — 2026-04-21

**Why:** Several rough edges after initial payment launch: `BillingPanel` crashed on open due to a leftover `setError` call. Expired/cancelled plans showed only a single locked "Renew Plan" button instead of the full plan picker. The payment confirmation page timed out after 30 seconds, which was too short for Razorpay webhook delivery in some regions.

**What:** `BillingPanel` crash fixed. Expired/cancelled plan state now falls through to the full plan grid with an expiry banner — users can pick any tier. `PlanPanel` adds "Get [Plan]" buttons for all tiers when the subscription is expired/cancelled. `PaymentSuccess` polling window extended to ~2 minutes.

**Changes:**
- `src/components/agentspace/BillingPanel.tsx`: removed stale `setError(null)` call that referenced an undefined setter — caused a crash on panel open
- `src/components/agentspace/NoActivePlanScreen.tsx`: removed the dedicated expired-plan screen that only showed a single "Renew Plan" button; expired state now renders the full plan grid with heading "Your plan has ended" and expiry date subtext
- `src/components/agentspace/PlanPanel.tsx`: removed single "Renew Plan" button from the current plan card; added `canRenew` condition in the all-plans grid that shows "Get [Plan]" buttons for all self-serve tiers when the subscription is expired or cancelled
- `src/pages/PaymentSuccess.tsx`: polling increased from 15 × 2s (30s) to 45 × 3s (~2 min); loading copy updated to "This can take up to a minute. Please don't close this page."

---

## [51] all-plans-self-serve — 2026-04-21

**Why:** Growth and Pro plans were gated behind "Talk to us" even though the Razorpay subscription flow was already in place for Trial/Starter. This blocked users from self-upgrading to higher tiers without a sales touch.

**What:** All four plan tiers are now purchasable directly via Razorpay checkout. The "Talk to us" CTA is removed from PlanPanel and replaced with "Get [Plan]" / "Upgrade now" / "Downgrade now" buttons for Growth and Pro cards.

**Changes:**
- `src/components/agentspace/PlanPanel.tsx`: `SELF_SERVE_TIERS` expanded from `{trial, starter}` to `{trial, starter, growth, pro}`; removed dead "Talk to us" fallback link from plan cards
- `src/components/agentspace/NoActivePlanScreen.tsx`: `SELF_SERVE_TIERS` expanded to all four tiers — Growth/Pro cards now render "Get Growth" / "Get Pro" pay buttons instead of mailto links

---

## [50] settings-restructure-plan-actions-billing — 2026-04-21

**Why:** The single `AgentSpaceSettingsPanel` with People/Plan tabs was hard to navigate and mixed member management with billing. Plan actions (switch, cancel) and a payment history view had no home. The settings entry point was a single "Settings" dropdown item instead of direct-access links.

**What:** `AgentSpaceSettingsPanel` is deleted and replaced by four focused slide-in panels (`MembersPanel`, `InvitesPanel`, `PlanPanel`, `BillingPanel`) accessible individually from the user dropdown. `PlanPanel` adds switch-plan (immediate Razorpay checkout or schedule-at-renewal) and cancel-subscription (now or at renewal) actions with inline confirm toggles. `BillingPanel` surfaces paginated `payment_transaction` history. A shared `SlidePanel` shell handles the slide-from-right animation and backdrop. `AgentSpaceHeader` replaces the single Settings button with four admin-only items.

**Changes:**
- `src/components/agentspace/SlidePanel.tsx` — new: shared backdrop + slide-from-right panel shell; props `open`, `onClose`, `title`, `subtitle?`, `children`; uses `AnimatePresence` + `motion.div` with `initial={{ x: '100%' }}`
- `src/components/agentspace/MembersPanel.tsx` — new: member list with role dropdown and remove action; extracted from `AgentSpaceSettingsPanel`
- `src/components/agentspace/InvitesPanel.tsx` — new: invite form + pending invite list with revoke; extracted from `AgentSpaceSettingsPanel`
- `src/components/agentspace/PlanPanel.tsx` — new: current plan card (status, period dates, cancel-at-renewal badge, used/remaining stats), cancel section with inline confirm toggle ("Cancel now" / "Cancel at renewal"), all-plans grid with switch buttons ("Upgrade/Downgrade now" + "At renewal") for non-current self-serve tiers, renew button for expired/cancelled subs, top-10 token history with test-session badge; no pagination
- `src/components/agentspace/BillingPanel.tsx` — new: paginated `payment_transaction` list (10/page); shows date, event label (Activated/Renewed/Cancelled/Failed), plan tier, amount (₹), status badge; fetch errors treated as empty state
- `src/components/agentspace/AgentSpaceHeader.tsx`: removed `onSettingsClick`; added `onMembersClick`, `onInvitesClick`, `onPlanClick`, `onBillingClick` props; replaced single Settings button with four admin-only dropdown items (Members/Users, Invites/UserPlus, Plan/CreditCard, Billing/Receipt); `TokenBalanceBar` wrapped in `hidden sm:block` to prevent mobile overflow; removed `overflow-hidden` from right-section container to allow dropdown positioning
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx` — deleted (all content moved to the four panels above)
- `src/pages/AgentSpace.tsx`: replaced `settingsOpen` state with `membersOpen`, `invitesOpen`, `planOpen`, `billingOpen`; wired all four panels into render; all three `AgentSpaceHeader` render sites updated with new props
- `src/lib/api.ts`: added `cancel_at_period_end?: boolean` to `AgentspaceSubscription` interface; added `cancelSubscription(token, {agentspace_id, at_period_end})`, `switchPlan(token, {agentspace_id, new_plan_tier, at_period_end})`, `fetchPaymentTransactions(token, agentspace_id, page, page_size)`; added `PaymentTransaction` interface `{id, created_at, event_type, plan_tier, currency, amount_paid, status}`

---

## [49] razorpay-only-payment-flow — 2026-04-21

**Why:** Stripe India moved to invite-only and blocks individual accounts from accepting payments in test mode. All payment flows route through Razorpay, which handles INR and currency conversion natively. The currency/region toggles in the UI added confusion without providing value.

**What:** Currency and region toggles removed from all three plan-selection surfaces. All payment calls hardcode `currency: "INR"`. Stripe checkout path remains gated behind `STRIPE_ENABLED=true` for future re-enablement.

**Changes:**
- `src/components/agentspace/NoActivePlanScreen.tsx`: removed `currency` state, `plansIntl` state and `getAllPlansIntl()` fetch, `India (₹) / Intl ($)` segmented toggle, Stripe checkout branch; `initiatePayment` always called with `currency: "INR"`
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx`: removed `planCurrency` state, `allPlansIntl` state and `getAllPlansIntl()` fetch, `₹ INR / $ Intl` toggle in All Plans section, Stripe checkout branch in `handleRenewClick`; plan grid sourced from `allPlansIn` only
- `src/sections/Pricing.tsx`: removed `region` state, `Region` type, `plansIntl` state and `getAllPlansIntl()` fetch, `India (₹) / International ($)` toggle; plan cards sourced from `plansIn` only

---

## [48] self-serve-payments — 2026-04-21

**Why:** Subscriptions were activated manually by admins via an internal API call. Users had no way to purchase or renew a plan without a human in the loop. Trial and Starter plans needed a self-serve checkout flow (Razorpay INR recurring + Stripe USD) with automatic activation on webhook confirmation.

**What:** End-to-end recurring subscription purchase flow added for Trial and Starter plans. Frontend initiates a payment intent via a new `POST /api/v1/payments/initiate` endpoint, opens the Razorpay modal (or redirects to Stripe Checkout for USD), and polls `GET /api/v1/payments/status` on a new `/payment/success` page until the webhook-triggered activation lands. Activation is handled exclusively by gateway webhooks (`subscription.charged` for Razorpay, `checkout.session.completed` for Stripe) via an internal helper that replicates the admin activation logic. A renew button is surfaced in `AgentSpaceSettingsPanel` for expired/cancelled subs. Overage events are emitted to Stripe Meters for Growth/Pro USD workspaces at session end.

**Changes:**
- `src/lib/razorpay.ts` — new: `loadRazorpayScript()` idempotent script loader; `RazorpayOptions` and `RazorpayInstance` interfaces; `Window.Razorpay` global declaration
- `src/lib/api.ts`: added `RazorpayCheckoutData`, `StripeCheckoutData`, `CheckoutData` union, `PaymentStatusResponse` types; added `initiatePayment(token, {agentspace_id, plan_tier, currency})` and `fetchPaymentStatus(token, provider, ref)`
- `src/pages/PaymentSuccess.tsx` — new: polls `fetchPaymentStatus` every 2s up to 15 attempts; renders loading spinner → success card (plan name + "Go to Workspace") → timeout error card with support mailto
- `src/App.tsx`: added `<Route path="/payment/success" element={<PaymentSuccess />} />`
- `src/components/agentspace/NoActivePlanScreen.tsx`: Trial/Starter CTA buttons wired to `handlePayClick` — opens Razorpay modal (INR) or redirects Stripe (USD); Growth/Pro remain mailto; loading state per tier; error banner below grid
- `src/sections/Pricing.tsx`: Trial/Starter CTAs navigate to `/agent-space` (authenticated) or `/auth` (unauthenticated) instead of opening a modal
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx`: added renew button for expired/cancelled subs in Plan tab; trial-expired renews as Starter; same `handleRenewClick` Razorpay pattern

---

## [47] calendly-demo-booking — 2026-04-16

**Why:** There was no direct path for a visitor (or logged-in user) to book a conversation with the founder. The Hero "Get a demo" and CTASection "Book a demo" buttons were inert, and the header nav had no persistent call-to-action for scheduling.

**What:** A new `BookDemo` section embeds the Calendly inline widget in a styled card at `#demo` on the landing page, placed between `SocialProof` and `FAQ`. The Hero and CTASection buttons are wired to scroll to this section. "Book a demo" is added to the global nav and to both the desktop and mobile logged-in user dropdowns in the Header (with a `Calendar` icon).

**Changes:**
- `src/sections/BookDemo.tsx` — new: `#demo` section with Calendar icon badge, heading/subtext, Calendly inline widget (`https://calendly.com/shreyash-singh/30min`) in a `rounded-xl border-gray-200 shadow-sm` card; Calendly script loaded idempotently via `useEffect`; `bg-gray-50` section background
- `src/pages/Landing.tsx`: imported and rendered `BookDemo` between `SocialProof` and `FAQ`
- `src/lib/constants.ts`: appended `{ label: 'Book a demo', href: '#demo' }` to `NAV_LINKS`
- `src/sections/Hero.tsx`: "Get a demo" button now smooth-scrolls to `#demo`
- `src/sections/CTASection.tsx`: "Book a demo" button now smooth-scrolls to `#demo`
- `src/components/layout/Header.tsx`: imported `Calendar`; added "Book a demo" button (with `Calendar` icon) to desktop profile popover and mobile logged-in menu — smooth-scrolls on `/`, navigates to `/#demo` from other routes

---

## [46] mail-subdomain-and-support-widget — 2026-04-16

**Why:** Resend sender addresses clashed with the Hostinger inbox on `@vivalyn.in`. Moving Resend to `mail.vivalyn.in` keeps the two mail paths cleanly separated. Separately, authenticated users had no in-app channel to reach support — the existing OTP contact form is public-only and too heavy for a logged-in user who just needs to file a quick ticket.

**What:** `submitSupport` API function added. A new `SupportModal` gives logged-in users a direct support form (type selector + message, no OTP) rendered via React portal so it escapes any header stacking context. A "Support" entry is wired into both the landing page header dropdown and the agent space header dropdown.

**Changes:**
- `src/lib/api.ts`: added `SupportTicketType` union type and `submitSupport(token, { type, message })` — `POST /api/v1/support/submit` with Bearer auth
- `src/components/ui/SupportModal.tsx` — new: modal with 2×2 type selector grid (Billing / Technical+Bug / Feedback / Other, each with icon + description), message textarea, submit → success flow; renders via `createPortal(…, document.body)` to avoid `backdrop-blur` stacking-context clipping
- `src/components/layout/Header.tsx`: imported `LifeBuoy` and `SupportModal`; added `supportOpen` state; "Support" button in desktop dropdown and mobile menu (logged-in only); `<SupportModal>` rendered as sibling to `<header>` via portal
- `src/components/agentspace/AgentSpaceHeader.tsx`: same `LifeBuoy` + `SupportModal` additions; "Support" button placed above Sign out in the user dropdown; `<SupportModal>` rendered inside the header return via portal

---

## [45] email-system-overhaul — 2026-04-16

**Why:** Plans are now public so the inquiry form needed a full rethink. The contact flow becomes a conversion-blocker capture form with OTP verification. The admin panel loses email-sending actions (now handled from the Hostinger inbox). The `inquiry` DB table is replaced by `contact_event`. Sender addresses are cleaned up across the board.

**What:** `InquiryModal` is rewritten as a 3-step form → OTP → success flow. `AdminInquiriesView` is replaced by `AdminContactEventsView` with new status values, type filter, expandable rows, and no email actions. `AdminDiscountsView` loses the send-email modal. `AdminHomeView` is updated to use `fetchContactEvents`. Pricing CTAs updated to "Talk to us".

**Changes:**
- `src/lib/api.ts`: removed `submitInquiry`, `sendAdminFollowupEmail`, `sendDiscountEmail`, `fetchAdminInquiries`, `updateAdminInquiry`, `Inquiry` interface; added `requestOtp`, `verifyOtp`, `fetchContactEvents`, `updateContactEvent`, `ContactEvent` interface
- `src/components/ui/InquiryModal.tsx` — full rewrite: 3-step `form → otp → success` with Framer Motion `AnimatePresence` step transitions; fields trimmed to name/email/message; OTP input (6-digit numeric, large centered); resend code with 429 rate-limit display; removed currency/business/plan fields
- `src/sections/Pricing.tsx`: CTA copy changed from "Contact Us" → "Talk to us" for Growth/Pro plans; removed `planInterest` pre-fill and `selectedTier` state
- `src/components/admin/AdminContactEventsView.tsx` — new file replacing `AdminInquiriesView`: status options `new|seen|replied|closed`; type filter `all|inquiry|support`; expandable rows showing full message + editable notes with save; status dropdown fix: removed `overflow-hidden` from table container, replaced broken per-row `ref` with document-level `mousedown` listener keyed to `statusDropdownId`; `onMouseDown` + `stopPropagation` on dropdown toggle and items
- `src/components/admin/AdminDiscountsView.tsx`: removed `SendEmailModal` component, `sendEmailTarget` state, "Send" button from discount rows, `sendDiscountEmail` + `Mail` imports
- `src/components/admin/AdminHomeView.tsx`: switched to `fetchContactEvents`/`ContactEvent`; updated status color map to `new|seen|replied|closed`
- `src/pages/AdminDashboard.tsx`: updated import `AdminInquiriesView` → `AdminContactEventsView`

## [44] retention-event-log — 2026-04-15

**Why:** The retention view's bar chart showed aggregate monthly sessions but had no plan history, no utilisation signal, and no way to see whether orgs were renewing, upgrading, or churning. With a new backend `subscription_event` table tracking all plan lifecycle changes, the frontend can now surface a meaningful retention picture.

**What:** `AdminRetentionView` is rebuilt with two new chart sections. A stepped line chart shows each org's plan tier progression over time (one line per org, Y-axis = tier rank Trial→Pro) — when orgs have no events yet, a placeholder is shown. A utilisation section renders per-org progress bars showing minutes used vs included in the current billing period, colour-coded by health (green / amber for expiring or heavy use / red for expired). The org table gains a Util. column. The monthly sessions bar chart is removed (covered by the Analytics view).

**Changes:**
- `src/lib/api.ts`: added `SubscriptionEvent` interface; extended `AdminRetentionOrg` with `minutes_included`, `minutes_used_this_period`, and `events: SubscriptionEvent[]`
- `src/components/admin/AdminRetentionView.tsx` — rebuilt:
  - removed `Bar`, `BarChart` recharts imports; added `LineChart`, `Line`
  - added helpers: `buildOrgTierByMonth`, `resolveTimeline`, `buildMonthRange` to transform event arrays into per-month tier rank series
  - `PlanTierTimeline` component: stepped `LineChart` per org with `type="stepAfter"`, Y-axis tick-formatted to tier names, colour palette cycling, inline legend; placeholder rendered when no events exist
  - `UtilizationSection` component: 2-col grid of per-org progress bars with `minutes_used_this_period / minutes_included` percentage, dynamic colour (emerald / amber / red), and `X / Y min` label
  - `OrgRow`: added Util. column showing utilisation percentage, amber highlight at ≥80%
  - table updated to 9 columns (added Util., removed monthly sessions chart)

---

## [43] test-token-fix-and-agent-guard — 2026-04-14

**Why:** Test sessions were charging the agentspace token balance the same as live sessions. Token history in the Settings panel and admin Org view didn't distinguish test from live transactions, making it impossible to audit real charges. Agent creation had no backend subscription guard — an expired plan couldn't stop a wizard submit. The settings panel header still read "Workspace Settings".

**What:** Token history now visually separates test and live transactions in both the member Settings panel and the admin Org view: test session rows render with a gray flask icon, a "Test" badge, and `0` delta. The two creation wizards (General and QnA) now detect a 403 subscription error from the backend and surface an amber "Renew to continue" banner with a mailto link instead of a generic red error. Settings panel header renamed to "Settings".

**Changes:**
- `src/components/agentspace/CreateAgentWizard.tsx`:
  - imported `ApiError` and `AlertTriangle`; added `isSubscriptionError` state
  - `handleEvalSubmit` catch: detects `ApiError.status === 403` → amber banner with `AlertTriangle` icon and "Contact us to renew." mailto link; red styling preserved for all other errors
- `src/components/agentspace/wizard/CreateQnAAgentWizard.tsx`:
  - same `ApiError` + `AlertTriangle` import, `isSubscriptionError` state, 403 detection, and amber banner
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx`:
  - header renamed "Workspace Settings" → "Settings"
  - added `FlaskConical` to Lucide imports
  - transaction rows: `test_session` reason → gray `FlaskConical` icon, inline "Test" badge, delta shown as `0` in gray; all other rows unchanged
- `src/components/admin/AdminOrgView.tsx`:
  - added `FlaskConical` to Lucide imports
  - token transaction table: `test_session` rows get gray flask icon, "Test" badge in reason cell, delta column shows `0` in gray

---

## [42] subscription-closed-loop — 2026-04-14

**Why:** The subscription system was missing its enforcement layer: expiring plans showed no warning, the admin panel had no visibility into which orgs were churning vs active, and the member dashboard gave no heads-up before a plan lapsed.

**What:** Member dashboard now shows an amber dismissible banner when `period_end` is within 7 days. A new Retention section in the admin panel provides per-org subscription health (plan, status, 30/60/90d session counts, last active date), a churn indicator for orgs that were using the product and then stopped, and a monthly sessions bar chart.

**Changes:**
- `src/pages/AgentSpace.tsx`: added `expiryBannerDismissed` state (reset on space switch); amber expiry banner rendered inside active-subscription dashboard when `period_end` ≤ 7 days away; dismissible via X button; added `AlertTriangle` to Lucide imports
- `src/lib/api.ts`: added `AdminRetentionOrg`, `AdminRetentionData` interfaces and `fetchAdminRetention()` function
- `src/components/admin/AdminSidebar.tsx`: added `'retention'` to `AdminSection` type; added "Retention" nav item with `TrendingDown` icon
- `src/pages/AdminDashboard.tsx`: added `retention: false` to mounted state; added `AdminRetentionView` section rendering
- `src/components/admin/AdminRetentionView.tsx` — **new**: 4 stat chips, monthly sessions BarChart (Recharts), org table with plan/status/period/session counts; churned orgs shown with `TrendingDown` icon and red row tint

---

## [41] minor-fixes — 2026-04-14

**Why:** Three small issues: (1) HowItWorks Step 6 (Report) had no dwell time — the sticky scroll zone height only covered 5 transitions, so reaching the last step immediately scrolled the user past it with no time to read. (2) QnA agent file uploads silently failed for PDFs larger than ~500 KB — `btoa(String.fromCharCode(...new Uint8Array(buf)))` spread the entire buffer as function arguments, exceeding the JS call stack limit; the unhandled rejection meant `setFiles` was never called and uploaded files never appeared, with no error shown. (3) READMEs predated the QnA agent type and were missing the new wizard components and endpoints.

**What:** HowItWorks scroll zone now uses `STEPS.length` (not `STEPS.length - 1`) so Step 6 gets a full 600px of dwell. QnA upload now uses a chunked base64 helper (8 192-byte slices) that avoids the stack limit for any file size; the loop is wrapped in try/catch to show a user-visible error on failure, and the input resets after each selection so the same file can be retried. `frontend/README.md` updated with QnA agent type table and wizard component tree.

**Changes:**
- `src/sections/HowItWorks.tsx`: scroll zone height changed from `(STEPS.length - 1) * SCROLL_STEP` to `STEPS.length * SCROLL_STEP`
- `src/components/agentspace/wizard/QnAResourceStep.tsx`: added `arrayBufferToBase64()` helper using 8 192-byte chunked loop; replaced inline `btoa(String.fromCharCode(...))` spread with helper call; wrapped file-processing loop in try/catch with `setFileError('Could not read file. Please try again.')`; added `e.target.value = ''` reset in `onChange` handler
- `README.md`: added Agent Types table (General vs QnA); expanded project structure to list QnA wizard components (`CreateQnAAgentWizard`, `QnAResourceStep`, `QnAQuestionReview`, `QnABehaviorStep`, `QnAConfigureView`)

---

## [40] howitworks-redesign — 2026-04-13

**Why:** The previous HowItWorks sticky-scroll layout felt disconnected — the RHS mockup floated separately from the steps, content was small and underutilized, and the auto-advance timer wasn't intuitive. The Why section also used high-contrast dark/colored tiles that stood out from the rest of the page's neutral palette.

**What:** Completely rewrote HowItWorks as a contained wizard-style stepper card. Six steps (Create → Voice & Language → Design Agent → Evaluation → Deploy → Report) each with a windowed macOS-style mockup panel (three-dot chrome + title bar) on the full RHS. Navigation is now scroll-driven — scrolling through the section advances the steps with direction-aware slide animation; clicking step circles smooth-scrolls to that step. Removed auto-advance timer and bottom prev/next bar. Compacted card height from 520px to 420px. WindowChrome content areas are scrollable with a minimal 4px scrollbar. Language tags in the Voice step show 7 languages + "+17 more" dashed pill. Updated ProblemSolution section to use neutral gray-50/white tiles (was gray-900/indigo-600) matching the page's standard palette.

**Changes:**
- `src/sections/HowItWorks.tsx` — full rewrite: 6-step wizard stepper card, WindowChrome mockups, scroll-driven step activation, direction-aware AnimatePresence transitions, scrollable panel content, compact 420px height
- `src/sections/ProblemSolution.tsx` — tiles changed from gray-900/indigo-600 to gray-50/white with border, matching page neutral palette

---

## [39] landing-page-redesign — 2026-04-13

**Why:** Landing page structure and sections needed a refresh for clarity and visual cohesion: merged the separate Problem/Solution sections into one unified side-by-side tile section, redesigned How It Works as a sticky-scroll layout with live mockups, removed the standalone Report Preview section, and cleaned up nav links to reflect the new structure.

**What:** Merged ProblemStatement + SolutionSummary into a single ProblemSolution section with two color tiles (gray-900 Problem + indigo-600 Solution) side-by-side. Rewrote HowItWorks as a sticky scroll layout — LHS steps highlight as user scrolls (useInView), RHS shows the active step's mockup (desktop); mobile stacks mockups below each step. Removed ReportPreview from Landing. Updated NAV_LINKS to replace Problem/Solution with "Why" (#why), added "Reviews" (#reviews). Removed Pricing frosted overlay — all 4 plans fully visible; Growth/Pro show "Reach out for better pricing" badge and Contact Us button. Trial card CTA cycles between "Request a Demo" and "Start Free Trial" via AnimatePresence.

**Changes:**
- `src/sections/ProblemSolution.tsx` — new section merging Problem + Solution into two color tiles
- `src/sections/HowItWorks.tsx` — full rewrite: sticky scroll with useInView step activation, animated RHS mockup panel (desktop), stacked mobile mockups
- `src/pages/Landing.tsx` — replaced ProblemStatement + SolutionSummary with ProblemSolution; removed ReportPreview
- `src/lib/constants.ts` — NAV_LINKS: replaced Problem/Solution with "Why" (#why); added "Reviews" (#reviews)
- `src/sections/Pricing.tsx` — removed frosted overlay on Growth/Pro; Trial card has AnimatePresence fade-cycling CTA labels; Growth/Pro show "Reach out for better pricing" text
- `src/sections/SocialProof.tsx` — added id="reviews" for nav anchor

---

## [38] batch-ui-fixes — 2026-04-13

**Why:** Nine accumulated UI/UX issues: settings panel clarity, pricing section discoverability, token gauge accuracy, landing page consistency, email completeness, and a pricing render bug.

**What:** Merged Members + Invites tabs into a single People tab. Redesigned user dropdown to include workspace settings access and user name. Softened token gauge colors to match indigo UI palette; fixed gauge to use plan minutes as reference instead of an arbitrary formula; added overflow counter for Pro scaling plans. Redesigned pricing section to show all 4 plans in a row with frosted overlay + inline CTAs on Growth/Pro. Fixed pricing section blank render on first load with loading skeleton. Normalized Hero section padding to match SectionWrapper rhythm; added scroll-margin-top to SectionWrapper to fix sticky-header anchor overlap. Fixed FeatureGrid mobile layout and compressed all feature descriptions.

**Changes:**
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx` — merged Members+Invites into People tab; panel title changed to "Workspace Settings"
- `src/components/agentspace/AgentSpaceHeader.tsx` — user dropdown shows name + email; Workspace Settings item added (admin only); standalone settings gear icon removed
- `src/components/agentspace/TokenBalanceBar.tsx` — softer gauge colors (indigo/amber/red-400); plan-aware gauge pct using minutesIncluded; overflow indicator for Pro scaling; light white tooltip
- `src/context/TokenContext.tsx` — extended with subscription data (minutesIncluded, scalingEnabled, overflowMinutes, hasSubscription) fetched in parallel with balance
- `src/sections/Pricing.tsx` — all 4 plans shown in single row; Growth/Pro use frosted backdrop-blur overlay with inline Contact Us CTA; loading skeleton fixes blank first-render; switched to getAllPlansIn/Intl
- `src/sections/Hero.tsx` — padding normalized from pb-60/pb-50 to py-16 md:py-24
- `src/components/ui/SectionWrapper.tsx` — added scroll-mt-[72px] for sticky header anchor fix
- `src/sections/FeatureGrid.tsx` — grid changed to grid-cols-1 sm:grid-cols-2 lg:grid-cols-4
- `src/lib/constants.ts` — compressed all FEATURE_CATEGORIES feature descriptions to max ~8 words

---

## [37] products-section-layout — 2026-04-11

**Why:** The Products section used a 3-column grid that became unbalanced with 4 agent types. The QnA badge in the agent list was embedded inline in the agent name cell, making it inconsistent with the rest of the table columns.

**What:** Switched the Products section to a single-row 4-column grid (`grid-cols-2 lg:grid-cols-4`) so all agent types appear side by side. Replaced the inline QnA badge in agent rows with a dedicated "Type" column in the agent list table, consistent with Language/Status/Created columns. Mobile card updated to show type as a pill alongside status and language badges.

**Changes:**
- `src/sections/FeatureGrid.tsx` — grid changed from `md:grid-cols-3` to `grid-cols-2 lg:grid-cols-4`; "Coming Soon" badge kept in card header, no overlay effects
- `src/pages/AgentSpace.tsx` — desktop agent grid updated to `grid-cols-[1fr_68px_90px_72px_100px_80px_72px_60px]`; "Type" column header added; inline QnA badge removed from agent name cell; type pill rendered as a standalone column cell (`QnA` indigo / `General` gray); mobile card shows type pill in the badges row

---

## [36] qna-agent-type — 2026-04-11

**Why:** The platform only supported open-ended General Agents. Edtech and institutional customers need reproducible, structured oral assessments — fixed question sets with a randomized pool, not free-form conversation — to ensure consistency and comparability across candidates.

**What:** Added a full QnA Agent type end-to-end: creation wizard (resource upload → AI question generation → question bank review → behavior config → evaluation → save), a new runtime session class that embeds the question bank into the system prompt with algorithmic randomization, post-creation question bank editing, and type-selection modal when creating an agent.

**Changes:**
- `src/components/agentspace/wizard/QnAResourceStep.tsx` — **new**: context textarea + drag-and-drop file upload (PDF/txt, max 3 files, 5MB); PDFs encoded as base64 strings; ThinkingPanel with rotating messages during generation
- `src/components/agentspace/wizard/QnAQuestionReview.tsx` — **new**: two-column layout (Fixed / Randomized Pool); inline question editing; move between pools; cross-question toggle (Zap icon); randomized count control; validation (≥1 fixed, ≥1 randomized, count ≤ pool size)
- `src/components/agentspace/wizard/QnABehaviorStep.tsx` — **new**: tone freetext input; feedback mode radio (silent vs brief feedback); ThinkingPanel during compilation
- `src/components/agentspace/wizard/CreateQnAAgentWizard.tsx` — **new**: 5-step wizard (language-voice → resource-upload → question-review → behavior-config → evaluation → done → configure); orchestrates `generateQnAQuestions → compileQnAAgent → saveQnAAgent`; inline EvaluationStep and DoneStep; QnA badge in header
- `src/components/agentspace/wizard/QnAConfigureView.tsx` — **new**: post-creation configure view; three tabs — Questions (two-column editor, saves via `updateAgentQuestionBank`), Profile (identity/session brief/behavior rules/guardrails with AI rewrite popups), Evaluation (metrics list, regenerate, curator prompt); `AiRewritePopup`, `QuestionBankEditor`, `QuestionItem` components inline
- `src/pages/AgentSpace.tsx` — type-selection modal added (two cards: General Agent / QnA Assessment Agent); `typeSelectOpen` + `createQnAAgentOpen` state; `CreateQnAAgentWizard` wired alongside existing `CreateAgentWizard`; configure overlay dispatches to `QnAConfigureView` or `AgentConfigureView` based on `agent.agent_type`; `ListChecks` icon added to imports
- `src/lib/api.ts` — `Agent` interface: added `agent_type: "general" | "qna"`; `agent_prompt` type widened to `AgentPromptSpec | QnAPromptSpec`; added interfaces `QnAQuestion`, `QnAQuestionBank`, `QnAPromptSpec`, `QnACompileResult`; added functions `generateQnAQuestions`, `compileQnAAgent`, `saveQnAAgent`, `updateAgentQuestionBank`
- `src/lib/constants.ts` — added `QnA Assessment Agents` to `FEATURE_CATEGORIES` (live, 4 features: structured question banks, AI question generation, reproducible assessments, cross-questioning); added `ListChecks`, `FileText`, `Repeat2`, `GitBranch` icon imports; footer product links updated to include QnA Assessment Agents

---

## [35] admin-panel-redesign — 2026-04-09

**Why:** The admin panel had grown organically from a usage/cost estimator into a multi-feature tool with no coherent layout. The date range filter bar (`7d/30d/90d + date inputs`) appeared on every tab including Inquiries, Subscriptions, and Discounts where it had no effect. Navigation mixed a flat top-tab bar with a breadcrumb simultaneously. There was no home/entry screen — the panel opened straight to analytics detail. Activating a subscription required knowing the exact agentspace UUID with no search. The flat tab structure was not scalable.

**What:** Full admin panel redesign to a left-sidebar SaaS layout. Date filter scoped exclusively to the Analytics section. New Home entry screen with live KPI cards, quick actions, and recent activity. Subscription activation replaced the UID input field with a searchable agentspace picker (by name) with a UUID fallback, admin profile display in results, and an agentspace detail card showing current plan/balance/trial eligibility. Billing cycle selector removed — subscriptions are monthly recurring until cancelled, so period is server-set. Admin Panel link added to the AgentSpace header avatar dropdown for super-admins (one-time `adminMe` check cached in `sessionStorage`).

**Changes:**
- `src/components/admin/AdminSidebar.tsx` — **new**: fixed left sidebar (220px) with Home / Analytics / Inquiries / Subscriptions / Discounts nav items and Sign out; active state uses `bg-indigo-50 text-indigo-700`
- `src/components/admin/AdminAnalyticsView.tsx` — **new**: wrapper that owns all analytics state (date filter, drill-down); renders date filter bar + breadcrumb at the top of analytics content only — not visible in any other section
- `src/components/admin/AdminHomeView.tsx` — **new**: entry screen; fetches 4 KPI stats in parallel (`Promise.all`); shows Active Subscriptions, Open Inquiries, Active Discount Codes, Sessions (30d); 3 quick-action buttons; recent Inquiries + Subscriptions lists with "View all" links
- `src/pages/AdminDashboard.tsx` — major rewrite: replaced flat top-tab bar + global filter bar with sidebar layout (`flex h-screen`); `activeSection` replaces `view`; date filter state removed (moved to `AdminAnalyticsView`); drill-down state removed; keep-alive pattern preserved for all 5 sections; `pendingActivateModal` flag wires Home quick-action to Subscriptions modal
- `src/components/admin/AdminSubscriptionsView.tsx` — `ActivateModal` redesigned: agentspace UID input replaced with `AgentspacePickerWidget` (search-by-name dropdown + UUID paste mode toggle); `AgentspaceDetailCard` fetches and displays current plan, status, balance, period, and trial eligibility after selection; admin name/email shown in search results; billing cycle picker removed — `period_start`/`period_end` no longer sent from frontend; `onSuccess` now calls `load()` to refresh the full list (prevents crash from missing computed fields like `balance`/`agentspace_name` on the raw activate response)
- `src/components/admin/AdminInquiriesView.tsx` — status filter tabs moved from top-right header to below the section title (left-aligned, `w-fit`); consistent section padding added
- `src/components/agentspace/AgentSpaceHeader.tsx` — added `isVivalynAdmin` state; calls `adminMe()` once on session load, caches result in `sessionStorage` key `vivalyn_is_admin`; "Admin Panel" link with `LayoutDashboard` icon added to user dropdown (super-admins only); renamed `isAdmin` → `isSpaceAdmin` to distinguish agentspace-role admin from Vivalyn super-admin
- `src/lib/api.ts` — added `AgentspaceSearchResult` (with `admin_name`, `admin_email`), `AgentspaceDetails` interfaces; added `searchAdminAgentspaces()` and `fetchAdminAgentspaceDetails()`; `activateSubscription` payload: `period_start`/`period_end` made optional

---

## [34] plan-config-unified — 2026-04-07

**Why:** Plan pricing, minutes, and cost floors were duplicated across multiple frontend and backend files with no single source of truth. `constants.ts` held four hardcoded plan arrays, `AdminSubscriptionsView.tsx` had its own `PLAN_MINUTES` dict, and the backend had the same data scattered across `admin.py` and `contact.py` — meaning any price change required edits in 5+ places with no guarantee of consistency.

**What:** All plan configuration now lives exclusively in `backend/main_web_server/plan_config.py`. The backend exposes it via a new public `GET /api/v1/plans` endpoint. The frontend fetches plan data from that endpoint on demand (module-level cache) and derives display arrays dynamically, replacing all hardcoded constants.

**Changes:**
- `src/lib/api.ts` — added `PlanConfigEntry` and `PlanConfigResponse` interfaces; added `fetchPlanConfig()` function (`GET /api/v1/plans`, no auth)
- `src/lib/constants.ts` — removed `PRICING_PLANS_IN`, `PRICING_PLANS_INTL`, `ALL_PLANS_IN`, `ALL_PLANS_INTL` hardcoded arrays; replaced with four async loader functions (`getPricingPlansIn`, `getPricingPlansIntl`, `getAllPlansIn`, `getAllPlansIntl`) that fetch from the API and transform to `PricingPlan[]`; module-level cache ensures single network request per session; `PLAN_NOTES` and `PLAN_NOTES_SHORT` handle tier-specific copy that is not pricing data
- `src/sections/Pricing.tsx` — replaced synchronous constant access with `useEffect` loading `getPricingPlansIn` and `getPricingPlansIntl` into component state
- `src/components/agentspace/NoActivePlanScreen.tsx` — replaced `ALL_PLANS_IN/INTL` imports with `useEffect` loading `getAllPlansIn` and `getAllPlansIntl`
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx` — replaced `ALL_PLANS_IN/INTL` imports with `useEffect` loading `getAllPlansIn` and `getAllPlansIntl`; added `allPlansIn` and `allPlansIntl` state
- `src/components/admin/AdminSubscriptionsView.tsx` — removed local `PLAN_MINUTES` dict; `ActivateModal` now fetches plan config on mount via `fetchPlanConfig()` and derives `planMinutes` map from the response

---

## [33] agentspace-rename — 2026-04-05

**Why:** Agentspace names were set at creation and permanently fixed — there was no way to correct a typo or rename a workspace as a team's usage evolved.

**What:** Admins can now rename their agentspace inline from the settings panel header. Clicking the workspace name (which gains a pencil icon on hover) enters edit mode; Enter or blur saves, Escape cancels. The new name is reflected immediately across the space switcher via `refetchSpaces`.

**Changes:**
- `backend/main_web_server/models/agentspace.py` — added `RenameAgentSpaceRequest` model
- `backend/main_web_server/routes/agentspaces.py` — added `PATCH /{agentspace_id}` endpoint; admin-only; validates non-empty, ≤80 chars; updates `agentspace` table
- `src/lib/api.ts` — added `renameAgentSpace(accessToken, agentspaceId, name)` function
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx` — added `isEditingName`, `nameInput`, `nameSaving`, `nameError` state and `nameInputRef`; added `handleSaveName` handler; panel header: workspace name is a clickable button with pencil icon (admin only) that enters an inline input on click; input auto-focuses and selects on mount; Enter/blur saves, Escape cancels; spinner shown while saving; error shown below if save fails

---

## [32] in-app-plan-details — 2026-04-05

**Why:** Inside the agentspace, users with no active plan saw a limited 2-card view (Trial + Starter only) gated behind an inquiry form — "email loop" friction that makes sense for anonymous landing-page visitors but not for signed-in workspace members who need to understand and act on plan options immediately. Growth and Pro details were completely hidden. The settings panel Plan tab had no plan comparison view, so even users already on a plan had no way to see what other tiers offered. Additional token rates and scaling details were absent from all agentspace-facing surfaces.

**What:** Replaced the `NoActivePlanScreen` plan cards with a full 4-plan grid (Trial, Starter, Growth, Pro) showing all details — minutes, sessions, additional token rate, scaling availability. Removed the InquiryModal from the agentspace plan request flow; all CTAs are now direct mailto links to `hello@vivalyn.in` pre-addressed with the plan name. Added an "All Plans" section to the settings panel Plan tab that is always visible (current tier highlighted, others shown for comparison), drawing currency from the active subscription or defaulting to INR. Extended `PricingPlan` in `constants.ts` to carry `additionalRate` and `scalingAvailable`, and added `ALL_PLANS_IN` / `ALL_PLANS_INTL` arrays with all four tiers.

**Changes:**
- `src/lib/constants.ts` — `PricingPlan` interface extended with `additionalRate: string | null` and `scalingAvailable?: boolean`; `tier` union widened to include `'growth' | 'pro'`; existing `PRICING_PLANS_IN/INTL` updated with `additionalRate: null`; Trial INTL price set to `$4.99 one-time` (previously INR-only); new `ALL_PLANS_IN` and `ALL_PLANS_INTL` arrays added with all four tiers and full pricing — Growth (₹4,499/₹4,999 INR · $69/$79 INTL), Pro (₹8,499/₹8,999 INR · $137/$159 INTL); Pro scaling overage rate set to `₹0.96/min` (INR) and `$0.018/min` (INTL) per pricing spec
- `src/components/agentspace/NoActivePlanScreen.tsx` — default (no-plan) state: replaced 2-card grid with 4-plan `ALL_PLANS_IN` grid, each card showing full details (price, billing, minutes, sessions, additional rate, scaling badge, note); CTAs are `<a href="mailto:...">` direct links, no InquiryModal; expired state: `InquiryModal` replaced with a mailto link; `InquiryModal` import and all related state removed
- `src/components/agentspace/NoActivePlanScreen.tsx` — added INR/Intl segmented toggle (defaults to INR); plans grid switches between `ALL_PLANS_IN` and `ALL_PLANS_INTL` based on selection; added "Custom pricing or queries" contact card at the bottom linking to `hello@vivalyn.in`
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx` — Plan tab: added `tierBadgeClass` helper; added `planCurrency` state (defaults to INR, auto-set to Intl when subscription loads with `currency: 'usd'`); "All Plans" section header includes INR/Intl segmented toggle; plan rows drawn from `ALL_PLANS_IN` or `ALL_PLANS_INTL` based on toggle; current tier highlighted; added "Custom pricing or queries" contact card above admin change-plan link; `InquiryModal` and `contactOpen` state removed; transaction history gated on `has_subscription`

---

## [31] subscription-none-500 — 2026-04-05

**Why:** `supabase.maybe_single()` returns `None` (not an `APIResponse` object) when no matching row exists. Both the subscription query and the balance query accessed `.data` on the result directly — crashing with `AttributeError: 'NoneType' object has no attribute 'data'` and returning a 500 on every agentspace that had never had a subscription record.

**What:** Added `None` guards before both `.data` accesses so the endpoint returns the correct `has_subscription: false` response instead of a 500.

**Changes:**
- `backend/main_web_server/routes/subscriptions.py` — `sub_result.data` check changed to `not sub_result or not sub_result.data`; `bal_result.data` access changed to `bal_result.data["balance"] if (bal_result and bal_result.data) else 0`

---

## [30] subscription-fixes — 2026-04-05

**Why:** Existing agentspaces opened normally without any plan — the subscription banner only appeared after data loaded and the `has_subscription: false` case wasn't blocking dashboard access. The "Tokens" settings tab predated the subscription model and surfaced an add-tokens form that no longer made sense. Pro+Scaling overflow was tracked on the backend but not surfaced in the UI. The FAQ still referenced the old pay-per-use model.

**What:** Gated the entire agentspace dashboard behind an active subscription. Replaced the inline subscription banners with a proper `NoActivePlanScreen` that renders when subscription is loading or inactive. Replaced the "Tokens" settings tab with a "Plan" tab showing subscription metadata, current balance, overflow (Pro+Scaling only), and transaction history. Removed the `onAddClick` affordance from `TokenBalanceBar` since there is no longer a self-serve add-tokens flow.

**Changes:**
- `src/lib/api.ts` — added `overflow_minutes: number` to `AgentspaceSubscription` interface
- `src/lib/constants.ts` — updated FAQ "What's the pricing model?" answer to mention Trial / Starter / Growth / Pro tiers and the ~10 min per session model
- `src/components/agentspace/TokenBalanceBar.tsx` — removed `onAddClick` prop and `Props` interface; component is now display-only (ring gauge + balance count + tooltip, no admin click handler or "Click to add tokens" tooltip line)
- `src/components/agentspace/AgentSpaceHeader.tsx` — removed `onAddClick` prop from `<TokenBalanceBar />`
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx` — replaced `'tokens'` tab with `'plan'` tab: lazy-fetches `fetchAgentspaceSubscription` on tab open; shows subscription info card (tier badge, status badge, period dates, minutes included / balance remaining grid, overflow card for Pro+scaling); admin-only "Contact us to change plan" button opens `InquiryModal`; transaction history section (paginated, moved from old tokens tab); removed add-tokens form, `addAmount/addLoading/addError/addOk` state, `handleAddTokens`, `refetchBalance`, `addTokens` import, `Coins`/`Plus` icons; added `fetchAgentspaceSubscription`, `AgentspaceSubscription`, `CreditCard`, `Calendar` imports
- `src/components/agentspace/NoActivePlanScreen.tsx` — **new**: three variants keyed on `subscription` prop — (1) no plan: two cards (Trial ₹200 + Starter ₹2,499/mo with crossed ₹2,999) each with request buttons that open `InquiryModal` with the relevant `planInterest`, plus "Need Growth or Pro?" contact link and pending-request note; (2) pending (`has_subscription=true, status=inactive`): Clock icon + "pending activation" message; (3) expired/cancelled: expiry date + "Renew Plan" button → `InquiryModal`
- `src/pages/AgentSpace.tsx` — added `subscriptionLoading` state (initialised `true`); subscription fetch effect now has a `finally` block setting `subscriptionLoading=false`; main content area gated: `subscriptionLoading` → centered spinner, `!has_subscription || status !== 'active'` → `<NoActivePlanScreen>`, otherwise → normal dashboard; removed old `AnimatePresence` subscription banners, `inquiryModalOpen` state, top-level `<InquiryModal>`, `AlertCircle` import; added `NoActivePlanScreen` import

---

## [29] subscription-system — 2026-04-05

**Why:** No pricing page existed beyond a placeholder, and there was no way to express interest in a plan or manage subscriptions. Agentspace members had no visibility into their plan status. The admin dashboard had no tooling for handling inquiries, activating subscriptions, or generating discount codes.

**What:** New pricing section with two public plan cards (Trial + Starter) and a "Contact Us" block for private Growth/Pro plans. Inquiry modal for all plan requests. Subscription status fetch and banners in the agentspace dashboard. Admin dashboard extended with three new tabs: Inquiries (with status management and follow-up email), Subscriptions (activate/renew with scaling toggle), and Discounts (code generator with live margin projector, usage drawer, send-email modal).

**Changes:**
- `src/lib/constants.ts` — new `PricingPlan` interface (`name, tier, price, crossedPrice, billingLabel, sessions, minutes, note?`); replaced legacy 5-plan array with Trial + Starter for both INR and INTL currencies; crossed prices permanent (not time-limited)
- `src/components/ui/InquiryModal.tsx` — **new**: `AnimatePresence` modal; fields: Name, Email, Business/Institute, Team size, Use case, Currency preference (INR/USD segmented control); optional `planInterest` chip pre-fill; success state with confirmation message; calls `submitInquiry` API
- `src/sections/Pricing.tsx` — redesigned: 2-card centred grid (`max-w-2xl`), crossed price display, "Get Started" → `InquiryModal`, "Contact Us to Explore More Plans" section below cards
- `src/lib/api.ts` — added `submitInquiry`; added `AgentspaceSubscription` interface (`has_subscription`, `plan_tier`, `status`, `currency`, `period_start/end`, `minutes_included`, `scaling_enabled`, `balance`); added `fetchAgentspaceSubscription`; added admin API functions: `fetchAdminInquiries`, `updateAdminInquiry`, `sendFollowupEmail`, `fetchAdminSubscriptions`, `activateSubscription`, `updateAdminSubscription`, `fetchAdminDiscounts`, `createDiscount`, `updateDiscount`, `sendDiscountEmail`, `getDiscountUsage`, `applyDiscount`, `projectDiscount`
- `src/pages/AgentSpace.tsx` — imported `fetchAgentspaceSubscription`, `AgentspaceSubscription`, `InquiryModal`; added `subscription` state + fetch effect; added inline subscription banners (indigo for no-plan, amber for expired, each with contact/renew button opening `InquiryModal`)
- `src/components/admin/AdminInquiriesView.tsx` — **new**: table with status dropdown (new / contacted / in_progress / closed), inline notes edit, follow-up email modal (subject + body HTML fields)
- `src/components/admin/AdminSubscriptionsView.tsx` — **new**: table with activate/renew modal (plan tier, currency, dates, notes, optional discount fields), scaling toggle (Pro only), cancel action
- `src/components/admin/AdminDiscountsView.tsx` — **new**: code generator form with live margin projector (effective price, per-session revenue, margin %, floor warning); code table with usage count; usage drawer (per-use records, total savings/revenue); send-email modal; deactivate/reactivate toggle
- `src/pages/AdminDashboard.tsx` — extended `View` union with `'inquiries' | 'subscriptions' | 'discounts'`; added nav tabs for all three; keep-alive mounted views for each new tab

---

## [28] token-credit-system — 2026-04-01

**Why:** Agentspace members had no visibility into their token balance and admins had no way to top it up. The admin dashboard tracked raw usage (seconds, LLM tokens, STT, TTS) but had no parallel view in credit units, no per-org balance gauges, and no transaction drill-down.

**What:** Added a token balance ring gauge to the agentspace header, a Tokens tab in the settings panel (balance display, add-tokens form, paginated transaction history), and a Usage/Tokens mode toggle across all three admin views. In token mode the admin KPI cards, org/agent tables, and runs table switch from raw usage metrics to credit units (1 credit = 1 min). The orgs overview table gains per-org ring gauges and current balance in token mode. The org detail view gains a collapsible Token Transactions section (fetched on expand, paginated). A `TokenContext` provider fetches and caches the active space's balance; `TokenBalanceBar` reads from it and re-uses an SVG ring gauge component (green/yellow/red thresholds based on `low_balance_threshold`).

**Changes:**
- `src/lib/api.ts` — added `TokenBalance`, `TokenTransaction`, `TokenTransactionsPage` interfaces; added `fetchTokenBalance`, `fetchTokenTransactions`, `addTokens` (member/admin agentspace endpoints); added `fetchAdminOrgTokenTransactions` (admin endpoint)
- `src/context/TokenContext.tsx` — **new**: `TokenProvider` fetches balance whenever `activeSpace` changes; exposes `balance`, `lowThreshold`, `balanceLoading`, `refetchBalance` via `useTokenBalance()` hook
- `src/components/agentspace/TokenBalanceBar.tsx` — **new**: SVG ring gauge (r=11, track gray-200, fill green/amber/red keyed to `balance > threshold*3 / > threshold / ≤ threshold`); credit count text beside ring; tooltip showing remaining credits and approx minutes; admin-clickable to open settings panel
- `src/components/agentspace/AgentSpaceHeader.tsx` — imported `TokenBalanceBar`; injected between Settings button and user avatar in the right section; passes `onSettingsClick` as `onAddClick`
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx` — added `'tokens'` to `Tab` union; imported `useTokenBalance`, `fetchTokenTransactions`, `addTokens`, `TokenTransaction`; added token tab state (transactions, pagination, add-form); `useEffect` loads transactions when tokens tab is active; `handleAddTokens` calls `addTokens`, refreshes balance via `refetchBalance`, reloads transaction list; tab bar extended to 3 items; Tokens tab content: balance card, add-tokens form with dummy-flow note, paginated transaction list with direction icon (ArrowUpRight/ArrowDownLeft), delta, balance_after
- `src/pages/AgentSpace.tsx` — imported `TokenProvider`; wrapped the `<AgentSpaceProvider>` children with `<TokenProvider>` so the context is available to all agentspace components
- `src/pages/AdminDashboard.tsx` — added `viewMode: 'usage' | 'tokens'` state; added Usage/Tokens segmented toggle (BarChart2 / Coins icons) to the filter bar; passes `viewMode` prop to `AdminOverviewView`, `AdminOrgView`, `AdminAgentView`
- `src/components/admin/AdminOverviewView.tsx` — added `OrgRingGauge` SVG component (r=9); added `viewMode` prop; batch-fetches token balances for all orgs via `Promise.all` after org list loads (failures default to 0 balance); tokens mode KPI row shows Total Credits Used, Avg Credits/Session, Orgs Tracked; orgs table in tokens mode replaces LLM/STT/TTS columns with Credits Used + Balance (ring gauge + number)
- `src/components/admin/AdminOrgView.tsx` — added `viewMode` prop; added token transaction state (txOpen, transactions, txPage, txTotal); `useEffect` fetches on expand; tokens mode KPI row shows Credits Used, Credits/Session, Agents; agents table in tokens mode shows Credits column instead of LLM/STT/TTS; collapsible Token Transactions section at bottom (ChevronDown toggle, paginated table: Date/Reason/Delta/Balance/Run)
- `src/components/admin/AdminAgentView.tsx` — added `viewMode` prop and `runCredits()` helper (`max(1, ceil(secs/60))`); tokens mode KPI row shows Credits Used and Credits/Session; runs table in tokens mode replaces STT/TTS columns with a Credits column showing per-run credit cost

---

## [27] remove-profile-is-admin — 2026-03-26

**Why:** `profile.is_admin` was a dead field — always `false`, never set `true`, and not used for any access control. The authoritative admin check is the backend `ADMIN_EMAILS` whitelist. Removing the field from the API interface eliminates a misleading property from `UserProfile`.

**What:** Removed `is_admin` from the `UserProfile` TypeScript interface. No component read or used this field so there are no call-site changes.

**Changes:**
- `src/lib/api.ts` — removed `is_admin: boolean` from `UserProfile` interface

---

## [26] admin-metrics-dashboard — 2026-03-23

**Why:** Per-session usage data was being recorded in `call_usage_record` but there was no interface to view it. Needed a secure admin-only dashboard for monitoring consumption, costs, and session transcripts across all organisations and agents.

**What:** Added a standalone `/admin` route with a hierarchical drill-down dashboard (overview → org → agent → run). Access is gated by a backend email whitelist — non-admins see an explicit "Access Denied" page rather than a silent redirect. Views use a keep-alive pattern (CSS `hidden`, not unmount) so navigating back is instant with no re-fetch. A pricing calculator shows cost ($ and ₹), revenue, profit, and margin — always visible at a glance, inputs collapsible behind "Edit inputs". Two time-series charts use the exact same pricing formula as the calculator so the numbers always agree. Installed `recharts` for all charts.

**Changes:**
- `package.json` — added `recharts`
- `src/App.tsx` — added `/admin` route → `AdminDashboard`
- `src/lib/api.ts` — added `AdminDailyPoint`, `AdminOverview`, `AdminOrgSummary`, `AdminAgentSummary`, `AdminRunSummary`, `AdminAgentDetail`, `AdminRunDetail` interfaces; added `adminMe`, `fetchAdminOverview`, `fetchAdminOrgs`, `fetchAdminOrgDetail`, `fetchAdminAgentDetail`, `fetchAdminRunDetail` functions
- `src/pages/AdminDashboard.tsx` — **new**: calls `adminMe` on mount; shows `NotAuthorized` page (Lock icon, message, back button) on 403; keep-alive view mounting (overview always mounted, org/agent lazily mounted then kept alive via CSS `hidden`, keyed by ID so switching entities re-fetches); breadcrumb nav; shared filter bar (7d/30d/90d quick range + custom date inputs + exclude-test toggle)
- `src/components/admin/AdminOverviewView.tsx` — **new**: 5 KPI cards (sessions with live/test split, session hours, LLM tokens with cached count, STT minutes, TTS chars); pricing calculator; usage charts; orgs table with drill-down on row click
- `src/components/admin/PricingCalculator.tsx` — **new**: summary cards always visible (total cost $/₹ with per-service breakdown, revenue, profit, margin with colour coding); inputs collapsed behind "Edit inputs" toggle; defaults match actual model pricing (gpt-5-mini conv, gpt-5 report, gpt-4o-mini-transcribe STT @ $0.003/min, inworld-tts-1.5-mini @ $5/1M chars); USD/INR exchange rate field; all values persisted to `localStorage`; exports `loadPricing()` for chart consumption
- `src/components/admin/UsageCharts.tsx` — **new**: sessions-over-time `LineChart`; revenue vs cost `LineChart` (dashed cost line, solid revenue line, exact per-token formula via `loadPricing()`); top-orgs horizontal `BarChart`; usage-breakdown `PieChart`
- `src/components/admin/AdminOrgView.tsx` — **new**: org-level KPI row aggregated from agent data; agent table with click-to-drill
- `src/components/admin/AdminAgentView.tsx` — **new**: paginated run list (20/page); Live/Test badge; all usage columns (turns, duration, LLM in/out, STT min, TTS chars); pagination controls
- `src/components/admin/RunDetailPanel.tsx` — **new**: Framer Motion slide-in from right with backdrop; LLM conv + report metrics; STT/TTS metrics; chat-bubble transcript (user: gray-100 left, agent: indigo-50 right); collapsible evaluation report rendered as formatted JSON; Escape key closes

---

## [25] language-grid-iso-badges — 2026-03-22

**Why:** Removing flag emojis left the language grid as plain text tiles with no visual identity — all tiles looked identical in weight and style, making it hard to scan at a glance.

**What:** Added a small ISO-639-1 code badge (`AR`, `EN`, `ZH`, etc.) above each language name in the grid. The badge uses `lang.key` which is already in the API response — no extra data needed. It adapts from `bg-gray-100 text-gray-400` to `bg-indigo-100 text-indigo-500` when the tile is selected, matching the existing tile highlight.

**Changes:**
- `src/components/agentspace/wizard/LanguageVoiceSelector.tsx` — language grid tiles changed from single-line text to a two-row layout: small uppercase `lang.key` badge on top, `display_name` below; tile layout changed from `flex items-center justify-center` back to `flex flex-col items-center gap-1.5`

---

## [24] voice-config-cleanup — 2026-03-22

**Why:** `voiceConfig.ts` exported `FLAG_MAP` (flag emoji lookup) and `PREF_LABELS` (`female1 → "Female 1"` etc.), both Wavenet-era artifacts. `FLAG_MAP` violated the no-emoji rule and was redundant since the language grid is fully data-driven from the `/api/v1/voices` API — no frontend-side language registry is needed. `PREF_LABELS` was designed for Wavenet's opaque voice IDs (e.g. `"en-US-Wavenet-D"`) where a label was the only gender signal; Inworld voices have real persona names ("Abby", "Brian") and the ♀/♂ badge already communicates gender — "Female 1" added nothing.

**What:** Deleted `voiceConfig.ts` entirely. Removed all flag emoji spans and `PREF_LABELS` sub-labels from both components that imported from it (`LanguageVoiceSelector` and `AgentConfigureView`).

**Changes:**
- `src/components/agentspace/wizard/voiceConfig.ts` — **deleted**
- `src/components/agentspace/wizard/LanguageVoiceSelector.tsx` — removed `FLAG_MAP, PREF_LABELS` import; removed flag `<span>` from collapsed language row and language grid tiles; removed `PREF_LABELS[voice.preference]` sub-label from voice cards
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — removed `FLAG_MAP, PREF_LABELS` import; removed flag `<span>` from the agent info row; removed flag prefix from `<option>` labels in the language `<select>`; removed `PREF_LABELS[voice.preference]` sub-label from voice cards

---

## [23] agentspace-pagination-and-creator-filter — 2026-03-18

**Why:** The agents table and run records table both fetched and rendered all records at once with no limiting. As agents and session history grow, this causes unnecessary data transfer and slow renders. There was also no way to filter agents by who created them.

**What:** Added client-side pagination (10/page) to the agents list with Prev/Next controls. Added a "Created by" filter dropdown that appears when 2+ unique creators exist in the agentspace, built as a custom animated dropdown (consistent with the existing agent-selector in RecordsTab). Updated `fetchAgentspaceRuns` to accept `page`/`pageSize` and return `{ runs, total }` matching the new paginated backend response. Added Prev/Next pagination controls to the RecordsTab that trigger a new fetch per page.

**Changes:**
- `src/lib/api.ts` — `fetchAgentspaceRuns(token, agentspaceId, page?, pageSize?)` now returns `Promise<{ runs: RunRecord[]; total: number }>` and appends `?page=&page_size=` to the URL; returns `{ runs: [], total: 0 }` on error
- `src/pages/AgentSpace.tsx` — added `agentsPage`, `createdByFilter`, `creatorDropdownOpen` state and `creatorDropdownRef` to `AgentSpaceContent`; added `uniqueCreators` memo (deduplicated, sorted creator names); added `useEffect` to reset `agentsPage` to 1 when `agentSearch`, `agentFilter`, or `createdByFilter` change; added `useEffect` for click-outside handling on creator dropdown; added creator filter dropdown in agents toolbar (renders only when `uniqueCreators.length > 1`, uses `User` icon + `AnimatePresence` panel); agents IIFE now applies `createdByFilter` filter, computes `pagedAgents` slice, renders pagination controls showing `X–Y of N` with disabled-aware Prev/Next buttons; added `runsPage` + `runsTotal` state to `RecordsTab`; `useEffect` now depends on `runsPage` and destructures `{ runs, total }` from `fetchAgentspaceRuns`; same Prev/Next pagination controls rendered below the runs table when `runsTotal > 10`; added `ChevronLeft`, `ChevronRight`, `User` to Lucide imports

---

## [22] agent-creation-flow-redesign — 2026-03-06

**Why:** The agent creation wizard had several UX and correctness problems: (1) The `'evaluation'` step was not in `STEP_DOTS`, so when active it made all dots go gray — a reset bug. (2) The evaluation step had a "Skip & Save" button and "sessions can still be recorded" dismissal text, framing it as optional when it should be mandatory. (3) After metrics generation, the 4 metric pill preview was shown with no value — cluttered noise before the done screen. (4) The `DoneStep` only had "Configure Prompt" and a disabled "Deploy Soon" button — no useful actions. (5) The planning/compiling thinking states showed a static spinner + oval pill badge — no sense of which agent was working, no progress feel. (6) Questions appeared abruptly with no anticipation moment and had circled-number prefixes (① ②) on suggestion buttons.

**What:** Rewrote the entire agent creation flow. Fixed the dots bug by removing `'evaluation'` as a `WizardStep` entirely — the full AI pipeline now runs within `step === 'planning'`. Made evaluation mandatory with no skip path. Skipped the metrics preview entirely, going straight to done after generation. Replaced the done screen with a 2×2 action tile grid mirroring the AgentSpace dashboard row (live link, test link, live/idle toggle, configure). Redesigned all thinking states with a typewriter animation (character-by-character typing with a blinking cursor) and a plain small-caps agent label with no pill/oval container. Added a questions reveal interstitial, a progress bar across questions, and a "Launching Prompt Compiler" micro-card after the last answer.

**Changes:**
- `src/components/agentspace/CreateAgentWizard.tsx` — removed `'evaluation'` from `WizardStep` union; removed `EvaluationStep` component; removed `evaluationCriteria`, `generatedMetrics`, `isGeneratingMetrics`, `isSavingAgent` state; `runCompiler` now sets `plannerStatus('awaiting_evaluation')` instead of `setStep('evaluation')`; added `handleEvalSubmit(criteria)` which sets `plannerStatus('generating_metrics')`, calls `generateEvaluationCriteria`, calls `saveAgent` with metrics, and sets `step('done')`; passed `onEvalSubmit` to `PlannerFlow`; dot `isPast` logic updated — `configure` step treated as past-all-done; `DoneStep` redesigned: emerald checkmark + agent name + live/idle badge, 2×2 grid of action tiles (`Link2` copy live link, `Copy` copy test link, `ToggleLeft/Right` live toggle via `toggleAgentStatus`, `Settings2` configure), full-width indigo "Done" button; copy tiles flash `Check` for 2000ms; imported `toggleAgentStatus`, `Copy`, `Link2`, `ToggleLeft`, `ToggleRight` from lucide/api
- `src/components/agentspace/wizard/PlannerFlow.tsx` — added `'awaiting_evaluation' | 'generating_metrics'` to `PlannerStatus` export; added `onEvalSubmit: (criteria: string) => void` to `Props`; replaced `ThinkingDots` (3 pulsing circles) with `TypewriterMessage` + `BlinkingCursor`; `TypewriterMessage`: 3-phase state machine (`typing` at 45ms/char → `holding` 1400ms → `erasing` → next message), cursor is a `w-px h-[1em] bg-indigo-400 motion.span` blinking `opacity [1,0,1]` at 0.8s; replaced `AgentBadge` (oval pill, `rounded-full`, indigo-50 bg) with `AgentLabel` (icon + `text-[10px] font-semibold uppercase tracking-[0.15em] text-gray-400`, no background or border); replaced `RotatingMessage` (cross-fade interval) with `TypewriterMessage`; `ThinkingPanel` layout changed to `gap-8 py-12` with `AgentLabel` above and typewriter below; added `revealingQuestions` interstitial — shows "I have N questions to refine your agent" card for 1200ms with pulsing `ChevronDown` before question flow slides in; `QuestionFlow` adds a thin 2px indigo progress bar above active question, removes circled-number prefixes from suggestion buttons, shows a "Launching Prompt Compiler" `Loader2` card for 500ms after last answer before calling `onComplete`; added `awaiting_evaluation` render: mandatory evaluation criteria textarea + "Generate Evaluation Criteria" button (no skip); added `generating_metrics` render: `AgentLabel("Evaluation Agent")` + `TypewriterMessage` with metrics messages; added `EvaluationForm` sub-component with local `criteria` state

---

## [21] behavior-rules-dict-schema — 2026-03-06

**Why:** The LLM prompt compiler always returned `behavior_rules` as a structured dict with semantic keys (`opening`, `probing`, `adaptation`, `feedback`, `closing`), but the frontend typed it as `string[]` and rendered it with a numeric index list. The `CollapsibleSection` component had no concept of labeled sub-sections. The AI rewrite flow for `behavior_rules` expected a JSON array back from the backend, and `applyAiRewrite` parsed only arrays and plain strings.

**What:** Changed `AgentPromptSpec.behavior_rules` from `string[]` to `Record<string, string>`. Added a `DictSection` component that renders each key as a labeled subsection with its own textarea, displayed in canonical order (`opening → probing → adaptation → feedback → closing`). Added `updateDictItem` helper. Widened `applyAiRewrite` to handle a `'dict'` type that parses a JSON object response. Updated all `applyAiRewrite` call sites to pass an explicit `'string' | 'list' | 'dict'` type tag.

**Changes:**
- `src/lib/api.ts` — `AgentPromptSpec.behavior_rules` changed from `string[]` to `Record<string, string>` with a clarifying comment (`// { opening, probing, adaptation, feedback, closing }`)
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — added `updateDictItem(key, field, value)` helper that merges a single dict key into the edited spec via spread; `applyAiRewrite` signature changed from `(key, isList: boolean, content)` to `(key, type: 'string' | 'list' | 'dict', content)` — `'dict'` path parses a JSON object and replaces the whole dict, silently no-ops on parse failure; all `applyAiRewrite` call sites updated (identity_and_persona: `'string'`, session_brief: `'string'`, behavior_rules: `'dict'`, guardrails: `'list'`); `CollapsibleSection` for `behavior_rules` replaced with new `DictSection` component that uses `onItemChange: (field, val)` instead of `(idx, val)`; `CollapsibleSection` for guardrails unchanged; added `DictSection` sub-component: renders keys in `BEHAVIOR_KEY_ORDER = ['opening', 'probing', 'adaptation', 'feedback', 'closing']` order (unknown extra keys appended after), each key shown as a `text-[10px] font-semibold uppercase tracking-wider text-gray-400` label above a `rows=2` textarea; collapse/expand + AI button header matches `CollapsibleSection` layout

---

## [20] profile-load-sequencing — 2026-03-06

**Why:** After clearing all DB rows, two concurrent calls to `GET /api/v1/profile/me` (React StrictMode double-fires effects) raced to create both the profile and the agentspace. The agentspace fetch in `AgentSpaceContext` fired before the profile creation finished — since profile creation is also what triggers default agentspace creation, the fetch returned an empty list and the UI showed a spinner indefinitely. Additionally, if profile creation failed entirely, the agentspace fetch would still proceed with a null profile.

**What:** Added a `!profile` guard to `loadSpaces` so the agentspace fetch only fires once both `profileLoading === false` and `profile !== null`. Added `profile` to the `useCallback` dependency array so the fetch re-triggers as soon as the profile becomes available.

**Changes:**
- `src/context/AgentSpaceContext.tsx` — `loadSpaces` bails early if `profileLoading || !profile` (was `profileLoading` only); `profile` added to the `useCallback` dependency array alongside `session?.access_token` and `profileLoading`; ensures agentspace fetch never fires while profile is still being created, preventing a race between default agentspace creation and the spaces list fetch

---

## [19] agentspace-mobile-layout — 2026-03-05

**Why:** The AgentSpace page was built desktop-first with no responsive breakpoints. The agents table (7 fixed columns, ~530px minimum width) and records table (6 columns) overflowed on mobile viewports. The RunDetailPanel was fixed at 500px, covering the whole screen on phones. The header avatar was oversized relative to the icon buttons at small widths.

**What:** Added full mobile responsiveness across the AgentSpace page using a dual-render pattern (mobile card + hidden desktop table row) for both `AgentRow` and `RunRow`. Table headers are hidden below `md`. Toolbars reflow correctly. Panels go full-width on mobile. Avatar scales down responsively.

**Changes:**
- `src/pages/AgentSpace.tsx` — main content padding: `px-4 md:px-6 py-4 md:py-6`; agents toolbar "Create Training Agent" button: `w-full md:w-auto justify-center` so it goes full-width when wrapped on mobile; agent table header: `hidden md:grid`; `AgentRow`: dual-render — mobile card shows agent name + persona + status/language badges in row 1, action buttons (copy live, copy test, toggle, configure) + date in row 2 (`md:hidden`), existing 7-column grid row prefixed `hidden md:grid`; records table header: `hidden md:grid`; `RunRow`: dual-render — mobile card shows name + score in row 1, email + type badge + date in row 2, agent name in row 3 (`md:hidden`), existing 6-column grid row prefixed `hidden md:grid`; Export CSV button label: `hidden sm:inline` (icon stays visible on all sizes)
- `src/components/agentspace/AgentSpaceHeader.tsx` — outer padding `px-3 md:px-5`; "Vivalyn" brand text: `hidden sm:block`; "/" separator: `hidden sm:block` (icon remains at all sizes)
- `src/components/agentspace/RunDetailPanel.tsx` — detail panel width: `w-full md:w-[500px]`; transcript panel positioning updated from `lg:` to `md:` breakpoints (`md:right-[500px] md:w-[420px] md:z-40`) to match the detail panel's breakpoint
- `src/components/ui/UserAvatar.tsx` — `sm` size changed from `w-9 h-9 text-sm` to `w-7 h-7 sm:w-9 sm:h-9 text-xs sm:text-sm` so the header avatar scales down on mobile to match the visual weight of adjacent icon buttons

---

## [18] records-transcript-panel — 2026-03-05

**Why:** The inline transcript load appended all chat bubbles at the bottom of the detail panel, creating an extremely long scrollable column. The "Load Transcript" button added unnecessary friction since transcript is always needed.

**What:** Removed the inline transcript section from `RunDetailPanel`. Transcript now auto-fetches on mount via `useEffect`. A compact "View Transcript" row (with message count badge once loaded) opens a separate `TranscriptPanel` — on desktop (≥ `md`) it slides in to the left of the detail panel so both are visible simultaneously; on mobile it slides over the detail panel as a full-screen overlay. Fixed a runtime crash where structured transcript content objects (`{why, type, excerpt}`) were rendered directly as React children.

**Changes:**
- `backend/main_web_server/routes/run_records.py` — added `GET /api/v1/agentspaces/{agentspace_id}/runs/{run_id}`: JWT auth + agentspace membership check; fetches single run record with full `transcript` field; verifies the run's agent belongs to the requested agentspace before returning
- `src/lib/api.ts` — added `fetchRunRecordById(token, agentspaceId, runId) -> RunRecord` (`GET /api/v1/agentspaces/{id}/runs/{runId}`, auth required); updated `RunRecord.transcript` content type from `string` to `string | Record<string, unknown>` to match actual DB payloads
- `src/components/agentspace/RunDetailPanel.tsx` — added `resolveContent()` helper that extracts a displayable string from structured content objects (checks `excerpt`, `text`, `content` keys before falling back to `JSON.stringify`); `TranscriptBubble` now calls `resolveContent(content)` instead of rendering `content` directly; removed `loadTranscript` button pattern; added `useEffect` that calls `fetchRunRecordById` on mount (auto-fetch, no user action required); replaced inline transcript section with a single "View Transcript" button row showing a spinner while loading, message count badge once loaded, or retry link on error; added `transcriptOpen` state; added `TranscriptPanel` sub-component (fixed positioned, `md:right-[500px] md:w-[420px] md:z-40`, slides over on mobile `z-50`, side-by-side on desktop); `TranscriptPanel` renders chat bubbles with user (right-aligned `bg-indigo-50`) and agent (left-aligned `bg-gray-100`) styles; rendered via `AnimatePresence` at root of `RunDetailPanel` return

---

## [17] records-detail-sidebar — 2026-03-05

**Why:** The Records tab showed sessions as table rows that expanded inline to reveal metric chips and a text summary. The experience was cramped, didn't support viewing transcripts, and buried the evaluation scores in a wall of chips with no visual hierarchy.

**What:** Replaced inline row expansion with a slide-in sidebar panel. Clicking any row opens `RunDetailPanel` from the right. The panel shows a pure-SVG radar chart for the 4 evaluation metrics, per-metric score chips (color-coded), summary, highlights, and areas to improve — all in a clean scrollable layout. A "View Transcript" entry point is included (implemented fully in [18]).

**Changes:**
- `backend/main_web_server/routes/run_records.py` — see [18] (same endpoint added)
- `src/lib/api.ts` — added `fetchRunRecordById` (see [18]); added `fetchAgentspaceRuns` already in [16]
- `src/pages/AgentSpace.tsx` — removed `expandedId` state; added `selectedRun: RunRecord | null` state; `RunRow` simplified from expand-toggle to `onSelect` callback; selected row highlighted with `bg-indigo-50/40`; `AnimatePresence` block at end of `RecordsTab` renders semi-transparent backdrop (`fixed inset-0 bg-black/10 z-30`) + `RunDetailPanel` when `selectedRun` is set; imported `RunDetailPanel`
- `src/components/agentspace/RunDetailPanel.tsx` — **new**; pure-SVG `RadarChart` sub-component: N axes evenly spaced, 3 background rings at 33/66/100% scale, indigo-600 fill polygon at 12% opacity, axis labels outside rings (truncated to 14 chars); overall score display (`text-4xl font-semibold`); per-metric chips with `scoreColor()` helper (emerald ≥8, amber ≥6, red <5); Summary, Session Overview, Highlights (`CheckCircle` icons), Areas to Improve (`ArrowRight` icons) sections; transcript entry point (see [18] for full implementation); slides in with `x: "100%" → 0` Framer Motion animation; close button + backdrop click both call `onClose`

---

## [16] agentspace-dashboard-redo — 2026-03-03

**Why:** The dashboard had two UX problems: (1) A redundant "Personal AgentSpace / Admin" block below the header duplicated information already visible in the header switcher. (2) The agents were displayed in a card grid with no way to see run records — admins had no visibility into session history, scores, or test-vs-live breakdowns without leaving the dashboard.

**What:** Removed the redundant header row. Added a small admin indicator (`Shield` icon) inline in the `AgentSpaceSwitcher` button. Converted the agents card grid to a compact list format. Added a two-tab layout ("Agents" / "Records") where the Records tab is a full-featured run records table with search, sort, type filter (All / Live / Test), inline row expansion to show per-metric scores and feedback, and CSV export.

**Changes:**
- `src/components/agentspace/AgentSpaceSwitcher.tsx` — added `Shield` from lucide-react; renders `<Shield className="w-3 h-3 text-indigo-400 shrink-0" />` inside the trigger button after the space name, visible only when `activeSpace?.role === 'admin'`
- `src/lib/api.ts` — added `agent_name: string` and `is_test: boolean` to `RunRecord` interface; made `transcript` optional (`transcript?`) since the new agentspace-level endpoint omits it for payload efficiency; added `fetchAgentspaceRuns(accessToken, agentspaceId) -> RunRecord[]` (`GET /api/v1/agentspaces/{id}/runs`, auth required)
- `src/pages/AgentSpace.tsx` — full rewrite: removed redundant space-name + role `<motion.div>` block; added `DashTab = 'agents' | 'records'` type with pill tab bar (`bg-gray-100 rounded-lg p-1 w-fit`); replaced `AgentCard` grid component with `AgentRow` list component (icon, name+language tag, persona, status badge, date, copy-live icon, copy-test icon, live toggle, configure icon — all in one `flex items-center` row); added `SortField`, `FilterType` types; added `SortIcon` helper component (outside render, accepts `field/sortField/sortDir` props); added `RecordsTab` component with search input, All/Live/Test filter pill buttons, sortable column headers (User, Agent, Date — click cycles asc/desc), `RunRow` component with `AnimatePresence` inline expand showing per-metric score chips, `transcript_summary`, and feedback bullets, "Export CSV" button (calls `exportCsv()` which builds a Blob and triggers download); `avgScore()` helper computes average of `evaluation_report.scoring` values; both data-fetching `useEffect`s restructured as inner async `load()` functions to avoid synchronous setState-in-effect lint errors

---

## [15] eval-tab-ux-simplification — 2026-03-03

**Why:** The Evaluation tab in the agent configure view had an awkward two-step flow: users typed a free-text criteria description into a textarea, clicked "Generate with AI" to produce metrics, then saw the results in a read-only preview card. This meant the AI output couldn't be edited without clearing and regenerating, and the "Describe criteria" textarea added unnecessary friction for users who already knew exactly which metrics they wanted.

**What:** Replaced the two-step flow with directly editable fields for metrics and eval prompt. Moved the AI generation capability to a small circular `Sparkles` button in the card header — the same pattern used by other section headers throughout the configure view. The AI button opens a compact popover where users type a brief criteria description; on submit it calls `generateEvaluationCriteria` and populates both editable fields, which can then be tweaked before saving.

**Changes:**
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — removed criteria `<textarea>` + description text + full-width "Generate with AI" button from `EvaluationTab`; removed read-only animated metrics preview card and "Clear" / "Save Metrics" buttons; replaced with: `EvalAiButton` sub-component (circular `w-7 h-7` sparkles button, opens `AnimatePresence` popover with "Describe what to evaluate" label + text input + submit + close, calls `generateEvaluationCriteria` on submit, populates parent state via `onGenerated` callback); `Metrics` card with editable badge list (each metric has an inline `X` remove button) + "Add a metric" input row (`Enter` or "Add" button); `Eval Prompt` card with directly editable `<textarea>` for `report_curator_prompt`; single "Save" button at the bottom persists `{ metrics: metricNames, report_curator_prompt: evalPrompt }` (or `null` if both empty) via `updateAgent`; state initialized directly from `initialMetrics` props

---

## [14] session-reporting-flow — 2026-03-02

**Why:** Four related problems needed fixing together: (1) The frontend closed the WebSocket immediately after `endSession()`, so the `session_report` message (sent by the backend after generating the evaluation) could never be received — the frontend polled a REST endpoint instead, which consistently returned 404 before generation finished. (2) Certain audio chunks from Inworld TTS had odd byte lengths, crashing `Int16Array` with `RangeError: byte length of Int16Array should be a multiple of 2`. (3) After session end, the UX was a jarring dark loading screen followed by an abrupt jump to `ReportScreen` — the design should show `EndedScreen` immediately, with a live spinner while the report generates and a "View Report" button once it arrives. (4) The error page used a `🔒` emoji which broke the UI design system, and the agent configure view had no way to edit evaluation metrics post-creation.

**What:** Added a `'reporting'` WebSocket phase to the session hook that keeps the connection open while the backend generates the report. Redesigned post-session page flow so `EndedScreen` appears instantly after call end. Fixed Int16Array crash. Replaced emoji with Lucide icon. Added Session/Evaluation tabs to the agent configure view with a full AI-powered metric editor.

**Changes:**
- `src/hooks/useAgentSession.ts` — added `'reporting'` to `SessionPhase` type; added `SessionReport` interface (`report: EvaluationReport | null`, `runId: string | null`) and `sessionReport` state; `endSession()` now stops mic + mic AudioContext only (WS stays open), sends `{"type":"end_session"}`, and sets phase to `'reporting'`; added `case 'session_report'` in WS message handler: sets `sessionReport`, transitions phase to `'ended'`, calls `cleanup()`; added safety `useEffect` — if phase stays `'reporting'` for >25s, force-transitions to `'ended'` (guards against backend failure); fixed `enqueueAudio()`: trims trailing odd byte from `rawBuffer` before `new Int16Array(buf)` to prevent `RangeError`; fixed `ws.onclose` handler: uses functional `setPhase` update so it reads the current phase correctly and doesn't reset a cleanly-ended session back to `'ended'` via incorrect branching
- `src/pages/AgentLive.tsx` — `ActiveSession` now renders `EndedScreen` (with `isGeneratingReport`) during the `'reporting'` phase instead of a dark full-screen spinner, so the session-ended card appears the moment the user hangs up; replaced `<span className="text-2xl">🔒</span>` with `<Lock className="w-6 h-6 text-gray-500" />` (lucide-react) on the error state; removed `'report-loading'` page state entirely; added `'report'` page state that renders `ReportScreen`; `handleEnded(turnCount, report)`: if `report.report` has `scoring` data → `setReportData(report.report)` and go to `'ended'`; if graceful end but no scoring (no evaluation configured) → go to `'ended'` directly; if `report === null` (abrupt disconnect) → set `isPollingReport = true` and go to `'ended'`; fallback poll `useEffect` runs only when `isPollingReport && pageState === 'ended'` — calls `fetchLatestRunRecord` up to 2 times with 3s gap, sets `reportData` if a scored record is found, always clears `isPollingReport` when done; `EndedScreen` receives `isGeneratingReport={isPollingReport}`, `reportAvailable={!!reportData}`, `onViewReport={() => setPageState('report')}`; `ReportScreen` rendered at `'report'` page state
- `src/components/agentlive/EndedScreen.tsx` — added optional props `isGeneratingReport?: boolean`, `reportAvailable?: boolean`, `onViewReport?: () => void`; renders `Loader2` spinner + "Generating your report…" text when `isGeneratingReport`; renders indigo "View Report" button with `FileText` icon when `reportAvailable`; default state (no evaluation) shows "Your conversation transcript has been saved." unchanged
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — added `Tab = 'session' | 'evaluation'` type with tab pill row below header (`inline-flex bg-gray-100 rounded-lg p-1`, active tab `bg-white shadow-sm`); "Save Changes" button is only rendered when `activeTab === 'session'`; added `EvaluationTab` sub-component: criteria `<textarea>` with descriptive placeholder, "Generate with AI" button (calls `generateEvaluationCriteria(accessToken, {task_definition, users_raw_evaluation_criteria})`, `Sparkles` + `Loader2` icons); animated metrics preview card (metric tags in `indigo-50/indigo-100` pills + curator prompt text, `motion.div` fade-in on arrival); "Save Metrics" button calls `updateAgent(token, agentId, {transcript_evaluation_metrics: metrics})`; "Clear" link calls `updateAgent` with `transcript_evaluation_metrics: null`; empty dashed-border card when no metrics are configured; imported `generateEvaluationCriteria` from api
- `src/lib/api.ts` — added `transcript_evaluation_metrics?: EvaluationMetrics | null` to `updateAgent` updates parameter type

---

## [13] evaluation-wizard-and-report-ui — 2026-03-02

**Why:** Agents could store `transcript_evaluation_metrics` in the DB, and the backend could generate and apply them, but there was no UI for setting them up during agent creation, no screen to display the resulting evaluation report after a session, and the API layer had no types or functions to support either operation.

**What:** Added the `EvaluationStep` to the agent creation wizard, built the full `ReportScreen` for post-session report display, and extended the API layer with evaluation types and functions.

**Changes:**
- `src/lib/api.ts` — added `EvaluationMetrics` interface (`report_curator_prompt: string`, `metrics: string[]`); added `EvaluationReport` interface (`scoring: Record<string, number>`, `highlights: string[]`, `transcript_summary: string`, `feedback: string[]`, `summary: string`); added `RunRecord` interface (`id`, `created_at`, `agent_id`, `user_email`, `user_name`, `transcript`, `evaluation_report`); added `generateEvaluationCriteria(accessToken, {task_definition, users_raw_evaluation_criteria}) -> EvaluationMetrics` (`POST /api/v1/agents/generate-evaluation-criteria`); added `fetchLatestRunRecord(agentId, email) -> RunRecord | null` (`GET /api/v1/agents/{id}/runs/latest`, returns `null` on 404 or network error); added `transcript_evaluation_metrics?: EvaluationMetrics | null` to `Agent` interface and `saveAgent` payload type
- `src/components/agentspace/CreateAgentWizard.tsx` — added `'evaluation'` as a 6th wizard step after `'done'`; added `EvaluationStep` sub-component: `<textarea>` with Cmd+Enter shortcut to trigger generation, `Sparkles`-icon "Generate Metrics" primary button (disabled while empty or generating), animated metrics preview (`AnimatePresence` + `motion.div` fade-in) showing metric tags in `indigo-50` + curator prompt text, "Save & Continue" / "Skip & Save" button (label switches based on whether metrics were generated); parent `CreateAgentWizard` state additions: `evaluationCriteria: string`, `generatedMetrics: EvaluationMetrics | null`, `isGeneratingMetrics: boolean`, `isSavingAgent: boolean`; `handleGenerate()` calls `generateEvaluationCriteria()` using `compiled.task_definition` and the entered criteria; `handleSaveAgent()` calls `saveAgent()` with `transcript_evaluation_metrics: generatedMetrics ?? undefined`; fixed scroll blocking bug in `EvaluationStep` — root `div` is `flex flex-col h-full`, inner scroller is `flex-1 overflow-y-auto`, content centred with nested `min-h-full flex items-center justify-center` so it centres when content is short and scrolls when tall
- `src/components/agentlive/ReportScreen.tsx` — **new**; full post-session evaluation report display; `scoreColors(score: number)` helper returns Tailwind bg/border/text class strings and a label (`Excellent ≥9`, `Good ≥7`, `Fair ≥5`, `Needs work <5`); layout: header with agent name, user name, turn count, and average score chip; overall summary prose block; score heatmap grid (staggered `motion.div` cards, each showing numeric score, colour-coded label, and metric name); highlights list (emerald `CheckCircle2` bullets); feedback list (indigo `ArrowRight` bullets); transcript summary section

---

## [12] session-ui-and-persona-name — 2026-02-25

**Why:** Four gaps remained after agent-live-ui ([11]) and voice-ui-abstraction ([10]): (1) The live session screen only showed the agent avatar — users had no visual representation in the call, making it feel one-sided. (2) `AgentPublicConfig` didn't expose the agent's persona name (e.g. "Arjun"), so the session screen always displayed the raw `agent_name` instead of the configured persona. (3) The `AgentCard` deploy section was gated to admins only, but link sharing is useful to all space members. (4) `voiceConfig.ts` still exported WaveNet-specific helpers (`VOICE_NAMES`, `getVoiceName()`) that were meaningless after the Inworld migration since Inworld voices are already named strings.

**What:** Added a `UserAvatar` component to the live session screen and redesigned `SessionScreen` into a two-card side-by-side layout (agent + user). Extended `AgentPublicConfig` with `persona_name` and wired it through `AgentLive` so the session displays the persona name. Removed the admin gate from `AgentCard`'s deploy section. Cleaned up `voiceConfig.ts`. Updated `AgentConfigureView.tsx` VoiceBar to persist the Inworld voice name as `agent_prompt.name` (top-level persona field) instead of the old `voice_character` sub-object.

**Changes:**
- `src/lib/api.ts` — added `persona_name?: string | null` to `AgentPublicConfig`; added `name: string` to `AgentPromptSpec` (top-level persona name, replaces the old `voice_character: {name}` sub-object pattern)
- `src/pages/AgentLive.tsx` — `ActiveSession` now passes `config?.persona_name ?? config?.agent_name` as `agentName` so the session screen uses the persona name; `SessionScreen` now also receives `userName` prop
- `src/components/agentlive/SessionScreen.tsx` — **redesigned**: two-card side-by-side layout (agent card + user card), each a frosted-glass `rounded-2xl border border-gray-700/60 bg-linear-to-b from-gray-800/70 to-gray-900/80 backdrop-blur-sm` panel; added `userName` prop; uses new `UserAvatar` component for user card; `isUserSpeaking` derived from `agentState === 'listening' && micEnabled`; user card subtitle shows "Speaking" / "Muted" / blank
- `src/components/agentlive/UserAvatar.tsx` — **new**; 144 px violet gradient circle with user initials (`getInitials()` handles multi-word names, max 2 chars); `isSpeaking`: two staggered emerald Framer Motion ripple rings (scale+opacity loop); `!micEnabled`: `MicOff` badge (bottom-right, `w-7 h-7 rounded-full bg-gray-800 border border-gray-700`) + circle dims to `opacity-60`
- `src/pages/AgentSpace.tsx` — `AgentCard`: removed `userRole` prop; deploy section (Go Live toggle + Copy Live Link + Copy Test Link) is now visible to **all space members**, not admin-only; Configure button rendered as a always-visible full-width bordered button (`border-indigo-200 text-indigo-600`)
- `src/components/agentspace/CreateAgentWizard.tsx` — `runCompiler()` now includes `name: selectedPersonaName` in the `agent_prompt` payload on auto-save; `DoneStep` replaces the old "Done" action button with a disabled "Deploy" button carrying a "Soon" badge, encouraging users to Configure first
- `src/components/agentspace/wizard/voiceConfig.ts` — removed `VOICE_NAMES` (WaveNet preference-to-persona-name map) and `getVoiceName()` helper; now only exports `FLAG_MAP` and `PREF_LABELS` — Inworld voice names are used directly as display names
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — `VoiceBar.handleUpdate()` now patches `agent_prompt: { ...editedSpec, name: personaName }` where `personaName = selectedVoiceName` (the Inworld voice name doubles as the persona name); `onUpdated` callback signature updated to `(lang, voice, personaName)`; `handleVoiceUpdated` in parent sets `edited.name = personaName` so the next "Save Changes" persists it; `displayPersona` shows `editedSpec.name ?? agentVoice`
- `src/components/agentspace/wizard/LanguageVoiceSelector.tsx` — voice cards now show `voice.voice_name` (Inworld name e.g. "Ashley") directly instead of `getVoiceName(...)` (which was WaveNet-specific)

---

## [11] agent-live-ui — 2026-02-23

**Why:** Agents could be configured and saved but had no way to be run by end-users. Admins needed a way to mark an agent live (or pause it) and share a direct link. External users needed a video-call-style interface to interact with a deployed agent via voice. Internal members needed a test link that bypasses the live/idle restriction so they can validate the agent before going live.

**What:** Added the complete agent live interaction layer: a public `/agent/:agentId` route with a video-call-style UI (animated agent avatar, real-time transcript panel, session controls), a `useAgentSession` hook managing WebSocket + mic + TTS audio, and AgentCard updates in the AgentSpace with Live/Idle status badge, Go Live toggle, and copy-link buttons.

**Changes:**
- `src/lib/api.ts` — added `agent_status: 'live' | 'idle'` to `Agent` interface; added `AgentPublicConfig` interface (`agent_name`, `agent_language`, `agent_status`); added `fetchAgentPublicConfig(agentId)` (no auth, `GET /api/v1/agents/{id}/public-config`); added `toggleAgentStatus(token, agentId, status)` (`PATCH /api/v1/agents/{id}/status`)
- `src/App.tsx` — added `/agent/:agentId` route pointing to `AgentLive` (no auth wrapper — public)
- `src/hooks/useAgentSession.ts` — **new**; manages full session lifecycle: WebSocket connection (`wss://.../ws/agent/{id}/session?email&name&mode&token`), mic capture (`AudioContext` 16kHz, `ScriptProcessorNode` bufferSize=1024, Float32→Int16 PCM binary frames), TTS playback (`AudioContext` 24kHz, gapless `BufferSource` scheduling via `nextPlayTimeRef`); state machine: `phase` (`connecting`→`active`→`ended`/`error`) + `agentState` (`listening`/`thinking`/`speaking`); React Strict Mode safe — uses `let mounted = true` per-effect guard so double-invoke in dev does not corrupt state; all handlers (`onmessage`, `onerror`, `onclose`) check `if (!mounted) return`; `session_ready` guards `ctx.resume()` with `state === 'suspended'` check and re-checks `mounted` after the `await`; `cleanup()` is idempotent (nulls all refs); `audio_done` polls `ctx.currentTime >= nextPlayTimeRef.current` to re-enable mic only after scheduled audio has finished; `sendAudioRef` stops mic sending during agent speaking to prevent echo; exposes `phase`, `agentState`, `transcript`, `micEnabled`, `toggleMic`, `endSession`, `error`
- `src/pages/AgentLive.tsx` — **new**; page state machine `loading → entry → session → ended / error`; loading: calls `fetchAgentPublicConfig`, blocks if live mode + `agent_status !== 'live'`; test mode: redirects to `/auth?next=/agent/${agentId}?mode=test` if no auth session; `ActiveSession` inner component mounts `useAgentSession` once join details are known — isolates hook from page-level re-renders; two-panel session layout: `SessionScreen` (flex-1) + `TranscriptPanel` (w-72 fixed RHS)
- `src/components/agentlive/AgentAvatar.tsx` — **new**; 120px circle with agent initials; `listening`: soft indigo pulse ring (Framer Motion scale+opacity loop); `thinking`: spinning dashed gray ring (`animate={{ rotate: 360 }}`); `speaking`: two expanding emerald rings with staggered delay
- `src/components/agentlive/SessionScreen.tsx` — **new**; video-call layout: sticky header (agent name + Live/Test badge), centered `AgentAvatar` with status label ("Listening…" / "Thinking…" / "Speaking"), live session timer via `useElapsed()` hook, mic toggle button, red End Call button (`PhoneOff` icon)
- `src/components/agentlive/TranscriptPanel.tsx` — **new**; scrollable RHS column; user messages: right-aligned indigo-600 bubbles (`rounded-br-sm`); agent messages: left-aligned white bordered bubbles (`rounded-bl-sm`); `AnimatePresence` + `fadeInUp` per entry; auto-scrolls to bottom via `bottomRef`
- `src/components/agentlive/EntryScreen.tsx` — **new**; centered card (`max-w-sm`); live mode shows email + name inputs; test mode shows name only (email from auth session); "Join Session" primary button calls `onJoin(email, name)` callback
- `src/components/agentlive/EndedScreen.tsx` — **new**; thank-you screen with `CheckCircle` icon, exchange count, and "transcript saved" notice
- `src/pages/AgentSpace.tsx` — updated `AgentCard`: new imports (`Check`, `Copy`, `Link2`, `ToggleLeft`, `ToggleRight`; `toggleAgentStatus`); `AgentCard` now accepts `userRole`, `token`, `onStatusChange` props; **status badge** always visible in card header (emerald-50/emerald-600 when live, gray-100/gray-400 when idle with 1.5px dot); **admin-only deploy section** below card body: Go Live toggle (`ToggleRight`/`ToggleLeft`), "Copy Live Link" button (indigo-600 filled), "Copy Test Link" button (ghost border); `handleToggleStatus` calls `toggleAgentStatus` then `onStatusChange(updated)` on success; `copyLink` writes to `navigator.clipboard` and shows "Copied!" for 2s via local state

---

## [10] voice-ui-abstraction — 2026-02-23

**Why:** Wavenet voice codes like `en-US-Wavenet-E` were leaking into the voice picker UI — users had no reason to see them. The abstraction layer (persona names like "Emma", "Ryan") already existed in `voiceConfig.ts` but the voice cards still displayed the raw internal code. Additionally the 2-column voice grid left each card mostly empty, wasting vertical space when 4 cards fit comfortably in a single row.

**What:** Removed all Wavenet code rendering from voice cards. Redesigned each card to show the persona name, a gender badge (♀/♂), and a PREF_LABELS subtitle ("Female 1", "Male 1", etc.). Switched both voice grids from 2-col to 4-col. Updated `api.ts` to drop the now-unused `tts_language_code` field from `LanguageVoiceOption`. The `voice_name` (Wavenet code) is still transported in the API payload and saved to the DB — it is simply never rendered.

**Changes:**
- `src/lib/api.ts` — removed `tts_language_code` from `LanguageVoiceOption` interface (field was unused by the frontend); `VoiceOption.voice_name` kept as the internal Wavenet code (not displayed in UI, used to populate `agent_voice` on save)
- `src/components/agentspace/wizard/LanguageVoiceSelector.tsx` — added `PREF_LABELS` import from `./voiceConfig`; removed `voice_name` display span; redesigned voice card: persona name (`getVoiceName`) + gender badge (`♀`/`♂`, pink-50/sky-50 colors, indigo-100 when active) + `PREF_LABELS[voice.preference]` subtitle; Preview button now full-width; grid changed `grid-cols-2 → grid-cols-4`; `selectedVoiceName` still derived from `voice.voice_name` for the `onContinue` payload
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — added `PREF_LABELS` import; removed `voice_name` display span; voice card redesign matches `LanguageVoiceSelector`; grid changed `grid-cols-2 → grid-cols-4`; `applyCurrentPref()` still matches by `v.voice_name === agentVoice` to correctly pre-select the current voice

---

## [9] agent-configure-view-redesign — 2026-02-23

**Why:** The initial `AgentConfigureView` squeezed content with a bottom AI button, had no way to change voice/language post-creation, used a uniform grid with poor visual hierarchy, had no collapsible sections for the list fields, clipped content on scroll, and was unusable on mobile.

**What:** Full redesign of `AgentConfigureView` with a sticky header, an inline `VoiceBar` for changing voice/language, a responsive two-column layout (LHS: string sections, RHS: collapsible list sections), a floating AI rewrite popover per section header, thin custom scrollbars globally, and full mobile responsiveness.

**Changes:**
- `src/index.css` — added global thin webkit scrollbar CSS: 4 px wide, transparent track, gray-200 thumb with rounded pill shape
- `src/components/agentspace/wizard/voiceConfig.ts` — **new**; shared constants file (`FLAG_MAP`, `VOICE_NAMES`, `PREF_LABELS`, `getVoiceName()`); extracted from `LanguageVoiceSelector.tsx` to satisfy the `react-refresh/only-export-components` lint rule that forbids non-component exports from component files
- `src/components/agentspace/wizard/LanguageVoiceSelector.tsx` — removed local constant definitions; imports `FLAG_MAP`, `getVoiceName` from `./voiceConfig`
- `src/components/agentspace/wizard/AgentConfigureView.tsx` — full rewrite:
  - **Props extended**: `agentLanguage: string`, `agentVoice: string`, `onAgentUpdated?: (updates: { agent_language, agent_voice }) => void`
  - **Header**: sticky `top-0 z-10` bar; on mobile scales to `text-sm`, `px-3 py-1.5 text-xs` button, subtitle hidden; on desktop restores full size
  - **VoiceBar** sub-component: info row shows flag emoji + persona name (extracted from `voice_character.name` via `/Your name is (.+?)\.?\s*$/` regex, falls back to `getVoiceName(lang, 'female1')`) + language subtitle; "Change Voice" expands an `AnimatePresence` drawer; drawer has a language `<select>` populated from `fetchVoices()` (cached after first open) + 4 voice cards with persona names, audio play/stop preview, active state in indigo; "Update Voice" calls `updateAgent()` with `agent_language`, `agent_voice`, and updated `voice_character`; pre-selects the current voice on open by matching stored wavenet voice name to preference key from the API response
  - **StringSection** sub-component: white bordered card with a header row (label + AI sparkle button) + `<textarea>` with `resize-none`
  - **CollapsibleSection** sub-component: header `<button>` toggles `AnimatePresence` height animation (`height: 0 → auto`); AI button uses `e.stopPropagation()` to avoid toggling; numbered list items with individual textareas
  - **AiButton** sub-component: `relative` container; sparkle button opens a floating popover (`w-72 max-w-[calc(100vw-2.5rem)]`, `absolute right-0 top-9 z-20`); instruction input + submit + close; calls `rewriteAgentSection()` on submit
  - **Two-column body**: `flex flex-col md:flex-row md:items-start`; LHS `w-full md:w-[55%]` with Identity & Persona (rows=10) + Task Definition (rows=7); RHS `flex-1` with Objectives (defaultOpen=true), Transcript (defaultOpen=false), Guardrails (defaultOpen=false); separator is `border-b md:border-b-0 md:border-r border-gray-100`
  - **Scroll model**: root is `min-h-full` (no overflow constraint); parent in `AgentSpace.tsx` is `overflow-y-auto`; sticky header pins correctly with this model
- `src/lib/api.ts` — added `updateAgent(token, agentId, updates)` for partial PATCH (`agent_prompt?`, `agent_language?`, `agent_voice?`); `updateAgentPrompt` now delegates to `updateAgent`
- `src/pages/AgentSpace.tsx` — configure overlay is `fixed inset-0 z-50 bg-white flex flex-col` with a 56 px top bar (back button left, agent name centred, close button right) and `flex-1 overflow-y-auto` body; `AgentConfigureView` receives `agentLanguage`, `agentVoice`, `onAgentUpdated`; `onAgentUpdated` updates both `configuringAgent` and `agents` list in-place

---

## [8] agent-creation-wizard — 2026-02-22

**Why:** Users needed a guided flow to create voice training agents from scratch — choose a language and voice, describe what the agent should do, let the AI plan all 5 prompt sections sequentially (asking clarifying questions only when meaningful), review and edit the compiled spec, then save.

**What:** Added the full agent creation pipeline: a multi-step wizard modal, a linear planner flow UI that shows section-by-section progress with inline Q&A, a language+voice selector with audio preview, and a post-compile configure step for review before saving. API layer wired to all new backend endpoints. Agent list grid with agent cards added to the AgentSpace page.

**Changes:**
- `src/lib/api.ts` — added `VoiceOption`, `LanguageVoiceOption` interfaces; added 7 API functions: `fetchVoices()` (`GET /voices`), `fetchVoicePreviewBlob(language, pref)` (`GET /voices/{lang}/{pref}/preview`), `planAgentSection(token, payload)` (`POST /agents/plan-section`), `compileAgentPlan(token, payload)` (`POST /agents/compile`), `rewriteAgentSection(token, payload)` (`POST /agents/rewrite-section`), `saveAgent(token, agentspaceId, payload)` (`POST /agentspaces/{id}/agents`), `fetchAgents(token, agentspaceId)` (`GET /agentspaces/{id}/agents`); added interfaces `AgentPromptSpec`, `PlanQuestion`, `PlanSectionResult`, `CompileResult`, `Agent`
- `src/components/agentspace/wizard/LanguageVoiceSelector.tsx` — **new**; searchable language grid (flag emoji + display name, 34 languages); collapses after selection; 4 voice cards per language showing WaveNet persona name, play/stop audio preview via blob URL + `<audio>` element with cancellation token pattern; active voice highlighted in indigo; "Continue" enabled only when both language and voice chosen; calls `onContinue(language, voicePreference, voiceName, personaName)`
- `src/components/agentspace/wizard/PlannerFlow.tsx` — **new**; progress display consumed during the planning phase; renders all 5 sections with status icons (`pending` ring, `planning` spinner, `needs_input` amber `?` badge, `done` green check); when `needs_input`, shows question text + 2 suggestion chips + free-text input; auto-scrolls to bottom as sections update
- `src/components/agentspace/CreateAgentWizard.tsx` — **new**; 6-step modal (`language-voice` → `prompt-input` → `planning` → `compiling` → `done` → `configure`); dot progress bar for the first 4 steps; `runPlanningPipeline()` iterates the 5 sections linearly — calls `planAgentSection()`, waits for user answers if `need_user_inputs`, appends to `plan_history`, then calls `compileAgentPlan()`; `handleAnswerQuestion()` appends `Q/A` lines to `plan_history` and resumes the pipeline; success screen with "Configure" (opens inline `AgentConfigureView`) and "Done" actions; slide transitions via `AnimatePresence`
- `src/pages/AgentSpace.tsx` — added `agents`, `agentsLoading`, `configuringAgent` state; `useEffect` fetches agents on active-space change; added `AgentCard` sub-component (bot icon, agent name, language badge, persona name, wavenet voice in monospace, creator + relative date, Configure + Deploy buttons — Deploy disabled with "Soon" badge); `EmptyAgentsState` sub-component; `extractPersonaName()` and `formatRelativeDate()` helpers; agent grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`; `CreateAgentWizard` mounted with `agentspaceId`; `handleWizardClose()` refetches agents after wizard closes

---

## [7] agentspace-management-system — 2026-02-21

**Why:** Users needed a full invite-based membership system — admins invite people by email, invitees get a branded email, click the link, sign in if needed, and accept to join. Admins needed a settings panel to manage members and invites. Users needed an inbox for invites sent to them.

**What:** Added the complete frontend layer: settings panel (members + invites management), inbox panel, invite accept page, API functions, header updates, and auth redirect support. Bell icon highlights only when there are pending invites.

**Changes:**
- `src/lib/api.ts` — added `AgentSpaceMember` and `Invite` interfaces; added 9 API functions: `fetchAgentSpaceMembers`, `changeAgentSpaceMemberRole`, `removeAgentSpaceMember`, `fetchAgentSpaceInvites`, `createInvite`, `revokeInvite`, `fetchInboxInvites`, `fetchInviteDetails` (no auth token), `acceptInvite`
- `src/App.tsx` — added `/invite/:inviteId` route pointing to new `InviteAccept` page
- `src/pages/Auth.tsx` — reads `?next=` query param via `useLocation`; after successful login/signup navigates to `next` if present instead of always going to `/agent-space`
- `src/pages/InviteAccept.tsx` — **new**; public page at `/invite/:inviteId`; fetches invite details without auth; handles states: loading, not found, expired/revoked, already accepted, pending+not-logged-in ("Sign in to accept" → `/auth?next=/invite/{id}`), pending+logged-in+email-mismatch, pending+logged-in+match (Accept button); on accept auto-redirects to `/agent-space` after 2 s
- `src/components/agentspace/AgentSpaceSettingsPanel.tsx` — **new**; right slide-out panel (max-w-440, `x: 100%→0` animation); Members tab with `MemberRow` (role dropdown + remove button, self-actions disabled) and Invites tab with send form (email + role select) and `InviteRow` (status badge + revoke button); admin-only
- `src/components/agentspace/InboxPanel.tsx` — **new**; right slide-out panel (max-w-420); `InviteCard` per invite with status badge, `timeAgo` timestamp, and Accept button for pending valid invites; on accept updates local state + calls `refetchSpaces()`; exports `usePendingInviteCount()` hook for the header badge
- `src/components/agentspace/AgentSpaceHeader.tsx` — added `onSettingsClick` and `onInboxClick` props; added `usePendingInviteCount` hook; bell button color is conditional (`pendingCount > 0` → indigo, else gray) with dot badge when pending; Settings button enabled only when `activeSpace?.role === 'admin'`
- `src/pages/AgentSpace.tsx` — added `settingsOpen` and `inboxOpen` state; wired both props to `AgentSpaceHeader`; renders `<AgentSpaceSettingsPanel>` and `<InboxPanel>`

---

## [6] profile-name-auth-hardening — 2026-02-20

**Why:** Three related gaps needed closing: (1) the signup form had no name field so email-signup users had empty `name` in their profile; (2) the `UserProfile` type didn't include `name`/`email` even though the backend now returns them; (3) a deleted Supabase user still appeared logged in because `getSession()` reads localStorage without server validation, and a stale agentspace ID in localStorage could cause the app to render a phantom profile. Additionally, first-time users could briefly see the "no agentspace" empty state due to a race between the profile-creation fetch and the agentspace fetch.

**What:** Added a name field to the signup form (hidden in login mode). Hardened the auth flow so a stale or invalid session is automatically cleared on the first API call. Sequenced agentspace loading to wait for profile fetch completion (profile creation also creates the default agentspace). Replaced the empty-agentspace CTA with a "Setting up your agentspace…" loader that auto-retries as a safety net.

**Changes:**
- `src/lib/api.ts` — added `name` and `email` to `UserProfile` interface; added `ApiError` class (extends `Error`, carries `status: number`); `fetchProfile` now throws `ApiError` instead of a plain `Error` so callers can inspect the HTTP status
- `src/context/AuthContext.tsx` — `signUpWithEmail` accepts a third `name` param; passes it as `options.data.full_name` to `supabase.auth.signUp` so name lands in `user_metadata` for profile creation to pick up
- `src/context/ProfileContext.tsx` — destructures `signOut` from `useAuth`; on `fetchProfile` failure, checks `e instanceof ApiError && e.status === 401` and calls `signOut()` to clear the stale localStorage session and redirect to auth; other errors still console-error only
- `src/context/AgentSpaceContext.tsx` — imports `useProfile`; `loadSpaces` bails early while `profileLoading` is true so agentspace fetch only fires after profile creation (and its default agentspace creation) completes; `profileLoading` added to `useCallback` deps
- `src/pages/Auth.tsx` — added `name` state; Name input field rendered only when `mode === 'signup'` (above Email); `switchMode` resets `name`; `signUpWithEmail` called with `name` as third arg
- `src/pages/AgentSpace.tsx` — `AgentSpaceContent` now reads `spaces` and `refetchSpaces` from context; added `useRef` retry counter (`retryCount`, max 5) with a `useEffect` that calls `refetchSpaces()` after 1500 ms whenever spaces are empty post-load (safety net for edge cases); replaced "You have no AgentSpaces / Create your first" empty state with a centered `Loader2` spinner + "Setting up your agentspace…" text

---

## [5] agentspace-management — 2026-02-20

**Why:** Users needed a workspace UI to organise agents. Each user gets one default AgentSpace on first login; they can create more. The app needed a dedicated in-app environment (no landing nav/footer) with a Supabase-style switcher so users can move between spaces.

**What:** Added the full AgentSpace management layer: API functions, a scoped context with localStorage persistence, a Supabase-style space switcher, a create-space modal, a custom header, and a redesigned AgentSpace page. The switcher lives in the top-left of a new per-page header; the last-used space is restored from `localStorage` on every visit.

**Changes:**
- `src/lib/api.ts` — added `AgentSpace` interface, `CreateAgentSpacePayload` interface, `fetchAgentSpaces(token)` (`GET /api/v1/agentspaces`), `createAgentSpace(token, payload)` (`POST /api/v1/agentspaces`); error detail extracted from FastAPI `detail` field on failure
- `src/context/AgentSpaceContext.tsx` — new; `AgentSpaceProvider` scoped to the AgentSpace page only (not global); fetches spaces on `session.access_token` change; restores active space from `localStorage` key `vivalyn_last_agentspace_id`, falling back to `spaces[0]`; exposes `spaces`, `activeSpace`, `spacesLoading`, `spacesError`, `switchSpace(id)`, `createSpace(payload)`, `refetchSpaces()` via `useAgentSpace()` hook; `createSpace` appends optimistically and switches immediately
- `src/components/agentspace/AgentSpaceSwitcher.tsx` — new; trigger button shows active space name with animated rotating `ChevronDown`; `AnimatePresence` panel (`opacity 0→1, y -4→0`, 150 ms) lists all spaces with an `Orbit` icon, active space highlighted in `indigo-50` with a `Check` icon; "New AgentSpace" button at the bottom in indigo; outside-click closes via `useRef + mousedown`
- `src/components/agentspace/CreateAgentSpaceModal.tsx` — new; `AnimatePresence` modal with blurred backdrop and scale+translate entrance (200 ms); single name input with `maxLength=80`, `autoFocus`; `Create` button disabled while name empty or submitting, shows `Loader2` spinner; error message displayed below input; resets state on close
- `src/components/agentspace/AgentSpaceHeader.tsx` — new; 64 px sticky header replacing the landing `Header` on this route; left side: logo button (`./icon.png` + "Vivalyn" wordmark) navigating to `/` on click, `/` separator, `AgentSpaceSwitcher`; right side: `Settings` icon (disabled, `opacity-40`, `cursor-not-allowed`, title "Settings (coming soon)") + `UserAvatar sm` with sign-out dropdown
- `src/pages/AgentSpace.tsx` — replaced; two-component pattern: outer `AgentSpace` handles auth guard and wraps `AgentSpaceProvider`; inner `AgentSpaceContent` consumes context, owns `createOpen` state; renders loading spinner, error message, active-space name with role subtitle, or empty-state CTA depending on context state; `staggerContainer + fadeInUp` animation on main content; `CreateAgentSpaceModal` always mounted, toggled by `createOpen`

---

## [4] profile-api-integration — 2026-02-19

**Why:** The avatar in the header and agent-space page was a hardcoded indigo circle with an email initial. Needed a real profile backed by the backend — showing the OAuth provider photo for Google/Microsoft users, and a unique gradient avatar for email users.

**What:** Added a backend API layer and a `ProfileContext` that fetches-or-creates the user's profile on login. Built a reusable `UserAvatar` component that renders the provider photo if available, falling back to the stored gradient with the email initial. Wired it into the header dropdown and the agent-space welcome screen.

**Changes:**
- `.env.example` — added `VITE_BACKEND_URL=http://localhost:8000`
- `src/lib/api.ts` — new; `fetchProfile(accessToken)` — typed fetch to `GET /api/v1/profile/me` using the Supabase access token as a Bearer header; exports `UserProfile` interface
- `src/context/ProfileContext.tsx` — new; `ProfileProvider` watches `session.access_token` from `AuthContext`, calls `fetchProfile` on login, clears on sign-out; exposes `profile` and `profileLoading` via `useProfile()` hook
- `src/components/ui/UserAvatar.tsx` — new; renders `<img referrerPolicy="no-referrer">` when `profile_pic_link` exists, otherwise a `<div>` with the stored CSS gradient and the first initial of the email; supports `sm` / `md` / `lg` sizes
- `src/main.tsx` — added `ProfileProvider` inside `AuthProvider`
- `src/components/layout/Header.tsx` — replaced hardcoded indigo avatar button with `UserAvatar size="sm"`; added `useProfile()` import
- `src/pages/AgentSpace.tsx` — replaced hardcoded indigo avatar `motion.div` with `UserAvatar size="md"`; added `useProfile()` import

---

## [3] supabase-auth — 2026-02-18

**Why:** The app needed user authentication to gate CTA actions (sign up, sign in) and prepare for user-specific features like a dashboard.

**What:** Added Supabase authentication supporting email/password, Google OAuth, and Microsoft Azure OAuth. Introduced React Router for multi-page routing, a global auth context, a login/signup page with animated form and OAuth buttons, and a simple protected dashboard. Header and CTA buttons now reflect auth state — showing "Sign in" when logged out and a user avatar dropdown when logged in.

**Changes:**
- `.env.example` — new; Supabase URL and anon key placeholders
- `src/lib/supabase.ts` — new; Supabase client singleton with env var validation
- `src/context/AuthContext.tsx` — new; React Context providing user/session state, email sign-in/sign-up, Google/Microsoft OAuth, sign-out, and `useAuth()` hook
- `src/pages/Landing.tsx` — new; extracted landing page content from App.tsx
- `src/pages/Auth.tsx` — new; login/signup page with tab toggle, Google & Microsoft OAuth buttons, email/password form, Framer Motion animations, subtle gradient background
- `src/pages/Dashboard.tsx` — new; protected welcome page with user avatar, email display, and sign-out
- `src/App.tsx` — replaced section composition with React Router routes (`/`, `/auth`, `/dashboard`)
- `src/main.tsx` — wrapped app with `BrowserRouter` and `AuthProvider`
- `src/components/layout/Header.tsx` — "Sign in" button navigates to `/auth`; logged-in users see initial avatar with dropdown (email + sign out); nav links prepend `/` when off landing page
- `src/sections/Hero.tsx` — "Request pilot pricing" renamed to "Get started free", navigates to `/auth`
- `src/sections/CTASection.tsx` — primary button renamed to "Get started" with auth gate; secondary button remains "Book a demo"

---

## [2] pricing-section — 2026-02-16

**Why:** Landing page needed a pricing section to communicate plan options for both Indian and international users, with a free trial available on a demo-request basis.

**What:** Added a Pricing section with an India/International region toggle, 5 pricing tiers (Trial, Starter, Growth, Pro, Business), and animated card transitions. Pro plan is highlighted as "Most Popular". Trial tier uses a "Request Demo" CTA instead of direct signup.

**Changes:**
- `src/lib/constants.ts` — added `PricingPlan` interface, `PRICING_PLANS_IN` and `PRICING_PLANS_INTL` data arrays
- `src/sections/Pricing.tsx` — new; region toggle, 5-column pricing card grid with AnimatePresence transitions, per-session cost, bonus credits, and "Contact sales" footer
- `src/App.tsx` — added `<Pricing />` section between ReportPreview and SocialProof

---

## [1] landing-page-v1 — 2026-02-16

**Why:** Vivalyn needed a public-facing landing page to communicate the product value prop (AI training & assessment agents), capture demo leads, and establish brand presence following the YC-grade clean SaaS design system defined in `UI.md`.

**What:** Built the complete landing page from a fresh React + Vite setup. Configured Tailwind CSS v4, Framer Motion, Lucide React, and Inter font. Created 6 reusable UI components, 2 layout components, and 9 landing page sections. Hero section features animated mock product UI cycling through 3 scenarios (Sales, Support, Education) with progress bar tabs.

**Changes:**
- `vite.config.ts` — added `@tailwindcss/vite` plugin
- `index.html` — added Inter font preconnect, SEO meta, updated title
- `src/index.css` — replaced with Tailwind v4 import + `@theme` for Inter font
- `src/App.tsx` — rewritten to compose all landing page sections
- `src/App.css` — deleted (all styling via Tailwind now)
- `src/lib/motion.ts` — new; shared Framer Motion variants (fadeInUp, staggerContainer)
- `src/lib/constants.ts` — new; all static data (nav links, features, steps, FAQs, metrics, testimonials, footer links)
- `src/components/ui/Button.tsx` — new; Primary/Secondary/Ghost variants
- `src/components/ui/Badge.tsx` — new; indigo-50 pill badge
- `src/components/ui/Card.tsx` — new; bordered card with hover lift
- `src/components/ui/SectionWrapper.tsx` — new; max-width + section padding wrapper
- `src/components/ui/Logo.tsx` — new; "Vivalyn" text logo with indigo icon
- `src/components/ui/Accordion.tsx` — new; FAQ expand/collapse with AnimatePresence
- `src/components/layout/Header.tsx` — new; sticky 72px header with blur backdrop, nav links, mobile hamburger
- `src/components/layout/Footer.tsx` — new; 4-column links grid + copyright
- `src/sections/Hero.tsx` — new; 2-column hero with animated scenario cycling (Sales/Support/Education), mock report card, transcript, progress bar tabs
- `src/sections/ProblemStatement.tsx` — new; centered narrow copy block
- `src/sections/SolutionSummary.tsx` — new; "What we do" section
- `src/sections/FeatureGrid.tsx` — new; 3-column card grid with Coming Soon badges
- `src/sections/HowItWorks.tsx` — new; 3-step horizontal timeline with connector line
- `src/sections/ReportPreview.tsx` — new; mock assessment report + feature bullet list
- `src/sections/SocialProof.tsx` — new; metrics banner, partner logos, testimonial cards
- `src/sections/FAQ.tsx` — new; 5 accordion items
- `src/sections/CTASection.tsx` — new; indigo-600 rounded container with inverted CTA
