Refer UI.md for UI style guide

Always follow consistent ui practises 

follow component structue as:

/components/page (name of the file/page which uses it)/ contains all the components for it
...
and keep the global components in /components itself

After each change update the CHANGELOG after asking.